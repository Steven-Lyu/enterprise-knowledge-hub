# 沉浸式错误案例分析-第 2 轮

最后更新时间：2026-07-24

对应代码轮次：第 2 轮｜数据库基础与 ORM 骨架

本文件用于配合第 2 轮代码学习，不是为了故意写坏代码，也不使用简单语法错误凑案例，而是模拟企业项目里真实容易出现的架构、逻辑和工程边界问题。

第 2 轮正确代码的目标是：

- 全项目只有一个 SQLAlchemy `Base`
- Web 请求和后续非 Web 运行入口共用同一套 Session 工厂
- ORM 通用字段统一放在 `app/models/base.py`
- Alembic、测试建表、脚本初始化都能通过稳定入口拿到完整 `Base.metadata`

## 错误案例 1：业务模型重新声明 Base，导致 Alembic 找不到表

错误类型：ORM 元数据注册逻辑错误 / 架构边界错误

### 1. 错误背景

第 3 轮开始写业务模型时，一个常见错误是开发者在每个模型文件里重新声明一次 `DeclarativeBase`。

错误代码示例：

```python
# app/models/document.py
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column


class Base(DeclarativeBase):
    pass


class Document(Base):
    __tablename__ = "documents"

    id: Mapped[str] = mapped_column(primary_key=True)
    title: Mapped[str]
```

这段代码本身可能不报语法错误，甚至 `Document` 类也能正常 import。真正的问题是：它没有继承项目统一的 `app.db.base.Base` 或 `app.models.base.BaseModel`。

### 2. 执行命令与错误表现

企业工程师通常会在生成迁移时发现异常：

```powershell
alembic revision --autogenerate -m "create document tables"
```

可能看到的输出：

```text
INFO  [alembic.runtime.migration] Context impl PostgresqlImpl.
INFO  [alembic.runtime.migration] Will assume transactional DDL.
Generating migrations/versions/20260724_create_document_tables.py ... done
```

表面看没有报错，但打开 migration 文件后会发现：

```python
def upgrade() -> None:
    pass


def downgrade() -> None:
    pass
```

这就是最容易误导新手的地方：命令成功了，但迁移内容是空的。

### 3. 第一视角排障路径

第一步，先确认 Alembic 使用的是哪个 metadata。

通常会去看 `migrations/env.py` 中类似这样的配置：

```python
from app.db.base import Base, import_all_models

import_all_models()
target_metadata = Base.metadata
```

第二步，用 Python 直接查看统一 `Base.metadata` 里有没有表。

验证命令：

```powershell
.venv\Scripts\python.exe -c "from app.db.base import Base, import_all_models; import_all_models(); print(list(Base.metadata.tables.keys()))"
```

错误输出示例：

```text
[]
```

第三步，检查业务模型继承关系。

如果看到业务模型文件里又写了一次：

```python
class Base(DeclarativeBase):
    pass
```

基本就能定位原因：业务表注册到了另一个 metadata 上，Alembic 看的却是 `app.db.base.Base.metadata`。

### 4. 根因分析

这是架构边界错误，不是语法错误。

SQLAlchemy 的 `Base.metadata` 可以理解为“所有 ORM 表结构的登记册”。如果项目里出现多个 Base，就等于出现多个登记册：

- Alembic 查的是登记册 A
- 业务模型登记到了登记册 B
- 所以 Alembic 生成迁移时看不到业务表

企业项目里这个问题很严重，因为它会导致：

- 迁移脚本漏表
- 测试数据库建表不完整
- 表关系无法统一解析
- 新同事不知道应该 import 哪个 Base

### 5. 修复方式

正确代码应该继承第 2 轮提供的统一基础模型：

```python
# app/models/document.py
from sqlalchemy.orm import Mapped, mapped_column

from app.models.base import BaseModel


class Document(BaseModel):
    __tablename__ = "documents"

    title: Mapped[str] = mapped_column(nullable=False, comment="文档标题")
```

同时在 `app/models/__init__.py` 聚合导入：

```python
from app.models.document import Document

__all__ = ["Document"]
```

### 6. 修复后验证

再次执行：

```powershell
.venv\Scripts\python.exe -c "from app.db.base import Base, import_all_models; import_all_models(); print(list(Base.metadata.tables.keys()))"
```

正确输出示例：

```text
['documents']
```

第 3 轮四个核心模型全部生成后，正确输出应接近：

```text
['documents', 'document_chunks', 'processing_tasks', 'document_events']
```

这才说明 Alembic 和测试建表入口能看到完整 ORM 表结构。

## 错误案例 2：`get_db_session()` 写成 return，导致 Session 生命周期失控

错误类型：数据库连接资源生命周期错误 / 请求依赖逻辑错误

### 1. 错误背景

第 2 轮已经生成了 `app/db/session.py`，其中最关键的函数之一是：

```python
def get_db_session() -> Generator[Session, None, None]:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

它看起来只是一个小函数，但它决定了每个 FastAPI 请求结束后数据库 Session 能不能被正确关闭。

一个常见错误是把它写成普通 `return`：

```python
from sqlalchemy.orm import Session

from app.db.session import SessionLocal


def get_db_session() -> Session:
    db = SessionLocal()
    return db
```

这个错误完全属于第 2 轮数据库基础设施范围，因为问题发生在当前已经生成的 `app/db/session.py`，不需要借用还没生成的 worker 代码来说明。

### 2. 执行命令与错误表现

当前第 2 轮还没有业务接口真正使用数据库，所以这个错误不一定会立刻爆炸。
但一旦后续 route 使用它：

```python
from fastapi import Depends
from sqlalchemy.orm import Session

from app.db.session import get_db_session


def list_documents(db: Session = Depends(get_db_session)):
    ...
```

高频请求后，可能看到数据库连接池耗尽：

```text
sqlalchemy.exc.TimeoutError: QueuePool limit of size 5 overflow 10 reached,
connection timed out, timeout 30.00
```

接口日志可能表现为前几次请求成功，后续请求开始失败：

```text
INFO request_id=req_001 path=/api/v1/documents status=200
INFO request_id=req_002 path=/api/v1/documents status=200
ERROR request_id=req_031 path=/api/v1/documents error="QueuePool limit of size 5 overflow 10 reached"
```

### 3. 第一视角排障路径

第一步，看到连接池耗尽后，先定位数据库 Session 生命周期。

错误关键字是：

```text
QueuePool limit of size 5 overflow 10 reached
```

这通常意味着连接被拿走后没有及时归还连接池。

第二步，查当前项目里的 Session 依赖入口。

命令：

```powershell
rg "def get_db_session" app
```

输出：

```text
app\db\session.py
```

第三步，打开 `app/db/session.py`，检查它是 `yield` 还是 `return`。

错误版本会看到：

```python
def get_db_session() -> Session:
    db = SessionLocal()
    return db
```

第四步，搜索 `db.close()` 是否存在。

命令：

```powershell
rg "db.close" app
```

错误输出可能为空：

```text

```

这说明请求结束后没有统一释放 Session。

### 4. 根因分析

这是资源生命周期错误，不是 SQL 查询错误。

`return db` 的问题是：

- 函数返回后，FastAPI 拿到了 Session
- 但 FastAPI 不知道请求结束后还要执行清理逻辑
- `db.close()` 没有稳定执行位置
- 请求多了以后，连接池里的连接逐渐被占住
- 最终出现连接池耗尽

企业项目里，数据库 Session 这类资源必须有明确生命周期：

- 什么时候创建
- 谁负责传递
- 什么时候提交或回滚
- 什么时候关闭
- 出异常时是否也能关闭

### 5. 修复方式

正确写法是保持第 2 轮现在的生成器依赖：

```python
from collections.abc import Generator

from sqlalchemy.orm import Session


def get_db_session() -> Generator[Session, None, None]:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

这里的重点是 `yield` 和 `finally`：

- `yield db`：把 Session 交给请求处理流程
- `finally: db.close()`：无论请求成功还是失败，请求结束后都关闭 Session

### 6. 修复后验证

第一步，确认 `get_db_session()` 是生成器依赖。

```powershell
rg "yield db|db.close" app\db\session.py
```

正确输出：

```text
app\db\session.py
```

第二步，用 Python 检查函数调用后返回的是生成器对象，而不是直接返回 Session。

验证命令：

```powershell
uv run python -B -c "from app.db.session import get_db_session; dep = get_db_session(); print(type(dep).__name__)"
```

正确输出：

```text
generator
```

如果错误地写成 `return db`，输出可能变成：

```text
Session
```

第三步，真实导入数据库模块。

验证命令：

```powershell
uv run python -B -c "from app.db.base import Base, import_all_models; import_all_models(); print(list(Base.metadata.tables.keys()))"
```

当前第 2 轮正确输出：

```text
[]
```

这里的空列表不是错误，因为第 3 轮才会生成业务表。

第 2 轮只要求 Base、Session、模型聚合入口能稳定存在。
