# 沉浸式错误案例分析-第 4 轮

最后更新时间：2026-08-13

对应代码轮次：第 4 轮｜Pydantic Schema 与统一响应结构

本文件只使用当前已经生成的 Pydantic Schema 和第 3 轮 ORM 模型，不提前借用
Repository、Service 或 API 路由。两个案例都能通过 Python 语法检查，但会破坏
ORM 到 API 的数据边界或已冻结的接口契约。

第 4 轮正确实现需要保证：

- Schema 可以读取 ORM 属性，但不会直接暴露整个 ORM 对象
- ORM 的 `chunk_metadata` / `event_metadata` 对外稳定输出为 `metadata`
- 文档列表 `page_size` 最大为 50，任务事件列表最大为 100
- 任务进度只能为 0-100，状态和步骤只能使用已冻结枚举
- 时间字段必须包含时区，并在响应中统一为 UTC
- 请求中的未知字段和拼错字段不能被静默忽略

## 错误案例 1：直接声明 metadata 字段，ORM 转响应时读到了 SQLAlchemy 的保留属性

错误类型：ORM 与 API 字段映射边界错误 / 响应序列化逻辑错误

### 1. 错误背景

第 3 轮为了避开 SQLAlchemy 声明式模型的保留名称，使用了不同的 Python 属性名：

```python
# app/models/document_chunk.py
chunk_metadata: Mapped[dict[str, Any]] = mapped_column(
    "metadata",
    MutableDict.as_mutable(JSON),
)
```

也就是说：

```text
Python ORM 属性名：chunk_metadata
数据库列名：metadata
API JSON 字段名：metadata
```

一个容易出现的错误 Schema 是：

```python
from typing import Any

from pydantic import BaseModel, ConfigDict


class WrongChunkResponse(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: str
    metadata: dict[str, Any]
```

开发者可能认为 `from_attributes=True` 会自动知道数据库列名，但它只会根据
Schema 字段名读取 Python 对象属性。

### 2. 第一步：确认 ORM 对象真正有哪些属性

执行：

```powershell
uv run python -B -c "from app.models import DocumentChunk; print(hasattr(DocumentChunk, 'chunk_metadata')); print(type(DocumentChunk.metadata).__name__)"
```

输出：

```text
True
MetaData
```

这里是问题的关键：`DocumentChunk.metadata` 不是某条 chunk 的扩展字典，而是
SQLAlchemy 基类上的表结构登记对象。

### 3. 第二步：模拟 ORM 转响应

```python
from types import SimpleNamespace

chunk = SimpleNamespace(
    id="chunk-001",
    chunk_metadata={"parser": "pypdf", "page": 1},
)

WrongChunkResponse.model_validate(chunk)
```

因为 `SimpleNamespace` 根本没有 `metadata` 属性，可能看到：

```text
pydantic_core._pydantic_core.ValidationError: 1 validation error for WrongChunkResponse
metadata
  Field required [type=missing, input_value=namespace(...), input_type=SimpleNamespace]
```

使用真实 `DocumentChunk` ORM 对象时，Pydantic 还可能错误读取继承来的
`DocumentChunk.metadata`，输出会更迷惑：

```text
metadata
  Input should be a valid dictionary
  [type=dict_type, input_value=MetaData(), input_type=MetaData]
```

### 4. 第三步：企业工程师第一视角排障

第一步，看错误路径，不要先怀疑数据库中 JSON 损坏：

```text
metadata -> Input should be a valid dictionary
input_type=MetaData
```

`MetaData` 是 SQLAlchemy 类型，不是数据库 JSON 内容，这提示问题发生在 ORM 属性读取阶段。

第二步，检查 ORM 映射名和数据库列名：

```powershell
rg -n "chunk_metadata|event_metadata|\"metadata\"" app\models app\schemas
```

应看到模型使用：

```text
chunk_metadata = mapped_column("metadata", ...)
event_metadata = mapped_column("metadata", ...)
```

第三步，检查错误 Schema 是否只写了：

```python
metadata: dict[str, Any]
```

如果没有 validation alias，Pydantic 不会从 `chunk_metadata` 取值。

定位模块：

```text
app/schemas/document.py
app/schemas/task.py
```

### 5. 根因分析

数据库列名和 Python 属性名是两个不同层面的概念：

- SQLAlchemy `mapped_column("metadata", ...)` 决定数据库列名
- ORM 类属性名 `chunk_metadata` 决定 Python 如何读取该值
- Pydantic `from_attributes=True` 按 Python 属性读取，不会反查数据库列名
- `metadata` 恰好又是 SQLAlchemy 声明式模型的重要保留属性

因此，仅仅让 API Schema 叫 `metadata` 并不能完成跨层映射。

### 6. 修复方式

正确 Schema 使用输入别名读取 ORM 属性，同时保持 API 输出名：

```python
from typing import Any

from pydantic import AliasChoices, Field


class DocumentChunkResponse(SchemaModel):
    metadata: dict[str, Any] = Field(
        default_factory=dict,
        validation_alias=AliasChoices("chunk_metadata", "metadata"),
        serialization_alias="metadata",
    )
```

事件 Schema 同理：

```python
metadata: dict[str, Any] = Field(
    default_factory=dict,
    validation_alias=AliasChoices("event_metadata", "metadata"),
    serialization_alias="metadata",
)
```

`AliasChoices` 允许两种合法输入：

- 从 ORM 对象读取 `chunk_metadata` / `event_metadata`
- 测试或内部代码直接传入 API 形式的 `metadata`

输出始终只有 `metadata`。

### 7. 修复后验证

执行：

```powershell
uv run python -B -c "from datetime import UTC, datetime; from types import SimpleNamespace; from app.schemas import DocumentChunkResponse; obj=SimpleNamespace(id='c1', document_id='d1', chunk_index=0, page_number=1, section_title=None, heading_path=[], content='text', char_count=4, chunk_metadata={'parser':'pypdf'}, created_at=datetime.now(UTC)); data=DocumentChunkResponse.model_validate(obj).model_dump(mode='json'); print(data['metadata']); print('chunk_metadata' in data)"
```

正确输出：

```text
{'parser': 'pypdf'}
False
```

验证结论：ORM 属性被正确读取，API 中只暴露冻结的 `metadata` 字段。

## 错误案例 2：分页和任务进度没有边界，非法请求通过 Schema 进入后续链路

错误类型：接口契约失守 / 资源消耗边界与状态一致性错误

### 1. 错误背景

一个只有类型、没有业务边界的 Schema 看起来很整洁：

```python
from pydantic import BaseModel


class WrongDocumentListQuery(BaseModel):
    page: int = 1
    page_size: int = 20


class WrongTaskStatus(BaseModel):
    task_id: str
    status: str
    progress: int
```

这段代码能接受整数和字符串，却没有落实冻结契约：

- 文档列表 `page >= 1`
- 文档列表 `page_size <= 50`
- `status` 必须是任务枚举
- `progress` 必须在 0-100

### 2. 第一步：验证非法分页被错误接受

```python
query = WrongDocumentListQuery(page=0, page_size=100000)
print(query.model_dump())
```

错误输出：

```text
{'page': 0, 'page_size': 100000}
```

Schema 没有报错，意味着非法参数已经穿过 API 契约层。

虽然第 5 轮 Repository 尚未生成，但后续常见分页换算是：

```python
offset = (query.page - 1) * query.page_size
limit = query.page_size
print(offset, limit)
```

输出：

```text
-100000 100000
```

负 offset 可能在数据库层报错，而超大 limit 会把资源保护问题推迟到数据访问层。

### 3. 第二步：验证非法任务状态被错误接受

```python
snapshot = WrongTaskStatus(
    task_id="task-001",
    status="almost_done",
    progress=140,
)
print(snapshot.model_dump())
```

错误输出：

```text
{'task_id': 'task-001', 'status': 'almost_done', 'progress': 140}
```

如果该对象用于 SSE，客户端可能收到：

```text
event: progress
data: {"task_id":"task-001","status":"almost_done","progress":140}
```

客户端进度条、终态判断和重连逻辑都无法可靠处理这种数据。

### 4. 第三步：企业工程师第一视角排障

第一步，确认非法值是在进入业务层之前还是之后产生。直接单独实例化 Schema：

```powershell
uv run python -B -c "from app.schemas import DocumentListQuery; print(DocumentListQuery(page_size=51))"
```

如果错误实现输出模型内容，而不是抛出 `ValidationError`，说明契约层没有保护边界。

第二步，查看 OpenAPI 或 Schema JSON 中是否真的存在上下限：

```powershell
uv run python -B -c "from app.schemas import DocumentListQuery; print(DocumentListQuery.model_json_schema()['properties']['page_size'])"
```

错误 Schema 可能输出：

```text
{'default': 20, 'title': 'Page Size', 'type': 'integer'}
```

其中没有：

```text
'minimum': 1
'maximum': 50
```

第三步，检查任务字段是否仍为裸 `str` 和裸 `int`：

```powershell
rg -n "status: str|progress: int|page_size: int" app\schemas
```

定位模块：

```text
app/schemas/document.py
app/schemas/task.py
```

### 5. 根因分析

Pydantic 类型校验只回答“是不是整数/字符串”，不会自动知道业务规则。

如果 Schema 不声明边界：

- API 文档不会展示允许范围
- 非法分页进入 Repository 后才失败或消耗大量资源
- 非法状态进入 SSE 后成为跨端协议不一致
- route、service、repository 可能分别补一套不同校验
- 相同错误在不同接口返回不同结果

企业项目先固定 Schema，核心价值就是把跨接口共用的输入边界集中在入口处。

### 6. 修复方式

分页字段显式声明范围：

```python
class DocumentListQuery(SchemaModel):
    page: int = Field(default=1, ge=1)
    page_size: int = Field(default=20, ge=1, le=50)
```

任务状态使用共享枚举，进度声明范围：

```python
class TaskStatusResponse(SchemaModel):
    task_id: str
    status: TaskStatus
    progress: int = Field(ge=0, le=100)
```

SSE 数据复用同一状态快照，而不是重新声明一组更宽松的字符串字段：

```python
class TaskSseData(TaskStatusResponse):
    pass
```

### 7. 修复后逐步验证

验证文档分页上限：

```powershell
uv run python -B -c "from app.schemas import DocumentListQuery; DocumentListQuery(page_size=51)"
```

正确错误输出中的关键行：

```text
pydantic_core._pydantic_core.ValidationError: 1 validation error for DocumentListQuery
page_size
  Input should be less than or equal to 50
```

验证任务进度：

```powershell
uv run python -B -c "from datetime import UTC, datetime; from app.schemas import TaskStatusResponse; TaskStatusResponse(task_id='t1', status='running', progress=101, timestamp=datetime.now(UTC))"
```

正确错误输出中的关键行：

```text
progress
  Input should be less than or equal to 100
```

验证未知状态：

```powershell
uv run python -B -c "from datetime import UTC, datetime; from app.schemas import TaskStatusResponse; TaskStatusResponse(task_id='t1', status='almost_done', progress=80, timestamp=datetime.now(UTC))"
```

正确错误输出中的关键行：

```text
status
  Input should be 'pending', 'running', 'success', 'failed' or 'cancelled'
```

检查 Schema 暴露给 OpenAPI 的约束：

```powershell
uv run python -B -c "from app.schemas import DocumentListQuery; print(DocumentListQuery.model_json_schema()['properties']['page_size'])"
```

正确输出应包含：

```text
{'default': 20, 'maximum': 50, 'minimum': 1, 'title': 'Page Size', 'type': 'integer'}
```

### 8. 补充验证：时间必须带时区

错误输入：

```python
datetime(2026, 8, 13, 10, 0)
```

正确 Schema 会拒绝并输出：

```text
Value error, 时间字段必须包含时区信息
```

合法输入：

```text
2026-08-13T10:00:00+08:00
```

序列化后的 UTC 输出：

```text
2026-08-13T02:00:00Z
```

## 第 4 轮排障结论

Schema 能创建实例不代表接口契约正确。第 4 轮需要重点检查的是：

- ORM 属性名、数据库列名、API 字段名是否被明确映射
- 请求参数上限是否真的进入 Pydantic JSON Schema
- 状态、步骤和事件类型是否复用同一套枚举
- 时间是否拒绝无时区值并统一到 UTC
- 响应模型是否只选择性暴露必要字段，而不是复制整张 ORM 表
- 列表和 SSE 是否严格遵守已经冻结的 JSON 结构
