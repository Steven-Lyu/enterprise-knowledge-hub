# Agents.md

本文件是 `企业知识库文档处理后端` 项目的专用协作说明，目标是在后续对话上下文被重置后，让新的 Agent 能快速理解：

- 这个项目为什么做
- 用户当前处于什么学习阶段
- 项目已经做到哪一步
- 后续应该按什么方式继续生成代码、解释代码和维护进度

相比总仓库级的 `Agents.md`，本文件更聚焦这个项目本身，不负责覆盖与本项目无关的长期背景。

## 1. 项目服务对象

本项目服务的核心用户不是普通互联网终端用户，而是：

- 企业内部知识库维护人员
- 内部工具团队 / 后端工程师
- 后续需要在同一项目上接入 RAG 的 AI 应用团队

项目目标不是做一个聊天 demo，而是做一个更接近企业真实模块的后端系统底座：

- 支持文档上传
- 支持元数据管理
- 支持异步解析与任务状态流转
- 支持 chunk 产出与后续 RAG 接入
- 支持缓存、SSE、错误处理、日志与排障

## 2. 用户背景简介

- 学校：温州肯恩大学
- 专业：计算机科学与技术
- 年级：大三结束，即将升大四
- 目标岗位：`AI应用开发 / Agent开发岗位`
- 非目标方向：不走算法岗，不以模型训练和科研为主线

用户当前已经完成一轮基础学习：

- Python 异步与 FastAPI 工程化
- Pydantic
- FastAPI 主线
- PostgreSQL / SQLAlchemy / Redis / Alembic
- Git 基础与协作

用户当前最需要的不是继续堆新概念，而是通过一个接近企业真实形态的项目，把这些基础真正串成工程能力。

## 3. 项目在用户整体路线中的位置

用户当前主线已经切换为：

1. 先做 `企业知识库文档处理后端`
2. 再在同一项目上扩展最小 RAG 能力

这意味着：

- 第一阶段重点是后端落地能力，不接 LLM
- 第二阶段才是 embedding、检索、问答、流式回答
- 当前所有设计都必须为后续 RAG 预留结构位，但不能在第一阶段就把范围做散

## 4. 当前项目进度

截至 `2026-08-13`，当前已完成“项目方案冻结”和前 4 轮代码生成：

- 已保留原始方案：`个人后端项目初步方案.md`
- 已整理进阶方案：`个人后端项目进阶方案.md`
- 已拆出代码分轮计划：`代码生成计划.md`
- 已完成第 1 轮代码：项目脚手架与运行入口
- 已完成第 2 轮代码：数据库基础与 ORM 骨架
- 已完成第 3 轮代码：文档、chunk、处理任务和事件流水四个核心模型
- 已完成第 4 轮代码：Pydantic Schema 与统一响应结构
- 已使用 uv 建立项目专用 Python 3.12 环境，并生成 `pyproject.toml` 与 `uv.lock`

第 1 轮已生成的核心文件包括：

- `app/main.py`
- `app/core/config.py`
- `app/core/logging.py`
- `app/core/enums.py`
- `app/core/exceptions.py`
- `app/core/auth_context.py`
- `app/api/v1/health.py`
- `app/api/v1/__init__.py`
- `app/api/__init__.py`
- `app/__init__.py`

第 2、3 轮已经补充的数据库与模型文件包括：

- `app/db/base.py`
- `app/db/session.py`
- `app/models/base.py`
- `app/models/document.py`
- `app/models/document_chunk.py`
- `app/models/processing_task.py`
- `app/models/document_event.py`
- `app/models/__init__.py`

第 3 轮已经落实的关键模型规则包括：

- 四张业务表共用同一个 `Base.metadata`，关系可以完整配置
- 文件哈希业务域唯一、文档版本和文件大小等基础约束已落到数据库
- 同一文档只允许一个 `pending/running` 活跃任务，同时保留已结束的历史任务
- 任务进度、重试次数、心跳和开始/结束时间具备明确字段与范围约束
- JSON 列使用可变容器跟踪，原地修改不会静默丢失
- `metadata` 数据库列通过非保留 Python 属性映射，避开 SQLAlchemy 保留名称
- 已创建 `沉浸式错误案例分析-第3轮.md`

第 4 轮已经补充的 Schema 文件包括：

- `app/schemas/common.py`
- `app/schemas/document.py`
- `app/schemas/task.py`
- `app/schemas/__init__.py`

第 4 轮已经落实的关键契约包括：

- 通用错误响应、可选成功外壳和泛型分页响应具有统一结构
- 文档列表分页最大为 50，任务事件分页最大为 100
- 上传元数据与 `UploadFile` 文件流职责分离
- ORM 的 `chunk_metadata` / `event_metadata` 对外稳定输出为 `metadata`
- 时间字段拒绝无时区值，并统一转换为 UTC
- 任务状态、解析步骤和 SSE 事件复用项目枚举，进度限制为 0-100
- 已创建 `沉浸式错误案例分析-第4轮.md`

当前已冻结的关键骨架决策包括：

- 任务运行形态：`Web 进程 + 独立 worker 进程`
- 状态机规则：文档状态与任务状态的明确流转
- 接口契约：分页、错误响应、SSE 事件、身份透传字段
- 版本/幂等规则：重复上传、新版本、重新解析三类场景
- 基础索引设计：文档、任务、事件、chunk 的关键索引
- chunk 元数据：为后续 RAG 预留页码、标题路径等字段

## 5. 当前执行方式

当前不采用“一次性生成完整项目代码”的方式，而采用：

- 按轮次生成
- 以 `.py` 文件为最小单位
- 按“由浅入深、从脚手架到细节、从总体架构到业务闭环”的顺序推进

当前代码生成计划存放在：

- `代码生成计划.md`

该计划已经明确：

- 每一轮生成哪些 `.py` 文件
- 每个文件分别负责什么
- 每一轮完成标准是什么

## 6. 对话触发规则

后续如果用户说：

- `接下来生成第1轮代码`
- `接下来生成第2轮代码`
- `接下来生成第n轮代码`

则表示：进入对应轮次的代码生成任务。

如果用户没有使用上述表达，而是继续问：

- 为什么这样设计
- 某个文件是做什么的
- 某段代码看不懂
- 企业里为什么这样实现

则默认视为一次提问，而不是继续生成下一轮代码。

## 7. 当前项目的高优先级理解点

在后续协作中，Agent 应优先帮助用户真正吃透以下内容：

- 项目分层：`api / schemas / services / repositories / db / core`
- 文档上传主链路
- 异步任务与任务状态流转
- worker 抢占、心跳、重试、失败回写
- PostgreSQL 与 Redis 的职责边界
- SSE 为什么适合长任务进度反馈
- chunk 为什么是后续 RAG 的关键桥梁
- 企业实现与学习 demo 的差异

## 8. 代码生成时必须遵守的标准

后续真正生成项目代码时，默认遵守以下标准：

1. 注释要尽可能详细，但不做逐行注释
2. 代码注释与文档字符串默认使用中文，除非用户明确要求英文或需要引用外部协议原文
3. 重点解释“企业里为什么这样实现”
4. 主业务逻辑不要塞进 route
5. 数据访问不要散落在 service 中
6. Redis 只能做加速层，不能做事实来源
7. 状态流转必须清楚、可解释、可排障
8. 代码生成必须严格按 `代码生成计划.md` 的轮次推进
9. 除非用户明确要求，否则不要越轮次生成后面的文件
10. 每轮代码生成完成后，都要给出一个推荐阅读顺序，明确先读哪个 `.py` 文件、再读哪个 `.py` 文件
11. 每轮代码生成完成后，推荐阅读顺序必须写入 `近期学习计划.md`
12. 每轮代码生成时，除正确代码外，还应同步创建或更新独立的 Markdown 错误案例分析文件，命名建议为 `沉浸式错误案例分析-第n轮.md`
13. 每轮错误案例分析文件至少包含 2 个有代表性的错误案例，且分析必须具体到可跟练的程度：包括错误代码片段、执行命令、每一步可能看到的终端输出或日志输出、企业工程师第一视角的排障路径、定位到的模块、原因分析、修复代码或修复方式、再次验证命令和正确输出样例
14. 错误案例必须至少达到逻辑级别或工程边界级别，优先聚焦架构偏差、资源生命周期、事务边界、状态流转、一致性、并发保护、缓存事实来源误用、worker 抢占不原子等问题；不能用简单语法错误、拼写错误、缺少 import、缩进错误、类型注解写错等低级问题凑数

## 9. Agent 的项目内职责

项目协作阶段，Agent 的职责应聚焦为：

1. 帮用户按轮次生成代码，而不是一次性堆完整项目
2. 每轮生成后，解释每个文件的职责与协作关系
3. 每轮生成后，给出该轮代码的推荐阅读顺序，并同步写入 `近期学习计划.md`
4. 明确指出企业写法与 demo 写法的区别
5. 在用户看不懂某段代码时，优先解释设计和职责，不只解释语法
6. 维护项目相关记录文件，保证日期、当前状态、下一步一致
7. 在每轮代码生成中主动组织沉浸式调试过程，让用户能看到典型错误、日志定位、模块排查、原因分析、修复验证和最终正确输出，而不是只给出最终可运行代码
8. 每轮沉浸式调试内容必须落到独立 Markdown 文件中，不能只写在对话回复里；对话回复可以摘要说明，但完整过程应保存在项目文件中，方便后续复习和上下文重置后继续衔接

## 10. 进度维护规则

只要本项目出现以下任一新进展，就要同步更新主记录文件：

- 新增或修改项目方案
- 新增或修改代码生成计划
- 完成某一轮代码生成
- 用户对某轮代码提出理解问题或阶段反馈

需要同步维护的文件：

- `学习进度追踪.md`
- `近期学习计划.md`
- 必要时更新本文件 `Agents.md`

更新时必须注意：

- 顶部日期要与当前对话日期一致
- 当前状态、当前任务、下一步不能互相矛盾
- 不能只在末尾追加，而不修正已经过期的描述
- 更新后在回复中明确提示：`学习进度已更新`

## 11. 当前默认下一步

如果用户没有改变方向，则默认下一步是：

- 按 `代码生成计划.md` 开始第 5 轮代码生成

第 5 轮的核心目标是：

- 建立 Document、Chunk、Task、Event 四类 Repository
- 把 SQLAlchemy 查询和批量写入从后续 Service 中隔离
- 为版本查询、分页过滤、任务抢占、心跳和事件时间线提供稳定数据访问接口
- 明确 Repository 负责数据访问，但不承担状态机业务判断和事务编排

## 12. 一句话总结

这是一个为 `AI应用开发 / Agent开发岗位` 准备的、偏企业真实形态的知识库后端项目。

当前阶段重点不是“尽快做完”，而是：

- 分轮生成
- 逐层吃透
- 边做边讲清楚企业实现逻辑
- 在同一项目上平滑过渡到后续 RAG
