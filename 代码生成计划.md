# 代码生成计划

最后更新时间：2026-07-16

本文件用于约束后续代码生成顺序，避免一次性写完整个项目。生成原则不是按时间切，而是按“由浅入深、从脚手架到细节、从总体骨架到业务闭环”的顺序推进。

## 协作约定

- 每一轮只生成当前轮次列出的模块代码，最小单位是 `.py` 文件
- 若你说：`接下来生成第n轮代码`，我将继续生成对应轮次代码
- 若你没有使用上述表达，而是继续提问，我将默认你是在问问题，而不是继续生成代码
- 若某一轮生成完后，你对代码细节、设计原因、企业实现差异有疑问，可以单独追问，不会自动进入下一轮
- 默认优先保证：结构正确、职责清楚、可运行、便于讲解；功能完整性按轮次逐步补齐

## 总体顺序说明

- 前几轮先把项目“能启动、能分层、能承载后续模块”的骨架搭起来
- 中间几轮补业务主链路：文档、任务、解析、状态、存储
- 再往后补企业化能力：缓存、SSE、错误处理、日志、测试
- 最后再进入第二阶段 RAG 扩展

## 第 1 轮｜项目脚手架与运行入口

目标：先把项目最外层骨架搭起来，保证服务能启动、配置能读、目录职责清楚。

本轮生成的 `.py` 文件：

1. `app/main.py`
   - 作为 FastAPI 应用入口，负责创建 app、注册路由、挂载生命周期与全局异常处理。
   - 后续所有模块都会通过这里组装，因此这一轮先把“能启动”定下来。
2. `app/core/config.py`
   - 负责读取环境变量和项目配置，统一管理数据库、Redis、存储目录等运行参数。
   - 这样后续代码不会到处散落硬编码配置。
3. `app/core/logging.py`
   - 提供项目统一日志初始化逻辑，先把日志格式、级别和基础字段入口搭起来。
   - 后面补 request_id、task_id 等字段时，就有固定承接点。
4. `app/core/enums.py`
   - 放第一批通用枚举，例如文档状态、任务状态、可见范围等基础常量。
   - 先集中定义，避免字符串状态散落在各层代码里。
5. `app/core/exceptions.py`
   - 放项目级基础异常类型，为后续统一错误响应和全局异常处理打底。
   - 这一轮先解决“异常怎么表达”，下一轮再补“异常怎么统一返回”。
6. `app/core/auth_context.py`
   - 提供从请求头提取身份透传信息的上下文对象或辅助方法。
   - 这样后续 service 层拿到的是结构化身份信息，而不是原始 headers。
7. `app/api/v1/health.py`
   - 提供最小健康检查接口，先验证服务路由可正常工作。
   - 第一版先返回应用存活状态，后续再扩 DB/Redis/storage 检查。
8. `app/api/v1/__init__.py`
   - 作为 v1 路由包入口，用于集中导出该版本下的路由模块。
   - 这样后续新增 documents/tasks 路由时不会让入口散乱。
9. `app/api/__init__.py`
   - 作为 API 包级入口，为版本化路由结构留统一挂载点。
   - 这是企业项目里常见的“分版本 API”基础骨架。
10. `app/__init__.py`
    - 作为应用包入口，保证整个 `app` 目录具备稳定包结构。
    - 这一文件本身逻辑不重，但对导入路径和工程整洁度有帮助。

本轮完成标准：

- FastAPI 服务可启动
- `/api/v1/health` 有最小可用响应
- 项目配置、日志、基础异常、身份透传上下文有最小骨架
- 能讲清为什么企业项目第一步不是直接写业务接口，而是先搭运行入口和基础设施

## 第 2 轮｜数据库基础与 ORM 骨架

目标：把数据库连接、Base、Session、通用模型基础设施搭好，为后续模型落表做准备。

本轮生成的 `.py` 文件：

1. `app/db/base.py`
   - 负责统一声明 SQLAlchemy Declarative Base，并集中导入所有模型注册元数据。
   - 这样 Alembic 和 ORM 初始化时能拿到完整模型集合。
2. `app/db/session.py`
   - 负责数据库引擎、session factory、依赖注入式 session 获取逻辑。
   - Web 层和 service 层后续都应通过这里拿 session，而不是各自 new 连接。
3. `app/models/__init__.py`
   - 负责聚合模型导入，保证 Base 元数据能感知到所有业务表。
   - 这是让迁移工具和 ORM 启动正常识别表结构的关键一层。
4. `app/models/base.py`
   - 放所有 ORM 模型共用的基础字段或 mixin，例如 `id`、`created_at`、`updated_at`。
   - 能减少重复定义，也更贴近企业里常见的模型继承写法。

本轮完成标准：

- SQLAlchemy Base、Session、数据库依赖注入具备最小可用形态
- 能说明 Web 层为什么不直接到处 new session
- 为 Alembic 和后续模型定义预留标准入口

## 第 3 轮｜核心数据模型

目标：先把最重要的业务实体落下来，定清文档、任务、事件、chunk 之间的关系。

本轮生成的 `.py` 文件：

1. `app/models/document.py`
   - 定义文档主实体，承载标题、部门、版本、状态、文件哈希、存储路径等核心信息。
   - 这是整个项目里最核心的业务对象，后续所有链路都会围绕它展开。
2. `app/models/document_chunk.py`
   - 定义文档分段实体，保存 chunk 内容、顺序、页码、标题路径等检索前置字段。
   - 第一阶段虽然不做 RAG，但这里已经为第二阶段接入 embedding 做准备。
3. `app/models/processing_task.py`
   - 定义解析任务实体，承载任务状态、进度、重试次数、心跳、worker 标识等字段。
   - 它是“异步任务系统感”的核心，不只是普通任务记录表。
4. `app/models/document_event.py`
   - 定义文档/任务事件流水，记录 queued、running、retrying、failed 等关键事件。
   - 后续排障、审计、讲项目里的“可追踪性”都会依赖这张表。

本轮完成标准：

- 四个核心实体关系明确
- 状态、版本、幂等、心跳、重试相关字段落到模型层
- 能从 ORM 层讲清项目为什么像企业模块而不是普通 CRUD

## 第 4 轮｜Pydantic Schema 与统一响应结构

目标：把请求、响应、分页、错误返回的契约固定下来，避免后面接口越写越散。

本轮生成的 `.py` 文件：

1. `app/schemas/common.py`
   - 放分页响应、通用成功响应、通用错误响应等公共 schema。
   - 这样所有接口在“壳子”上先统一，不会一个接口一个风格。
2. `app/schemas/document.py`
   - 定义文档上传、文档详情、文档列表、chunk 查询等和文档相关的请求响应模型。
   - 这里会把接口契约和 ORM 结构做一次干净隔离。
3. `app/schemas/task.py`
   - 定义任务详情、任务事件、任务状态、SSE 数据载体等和任务相关的 schema。
   - 这样后续 SSE 和普通查询都能复用同一套结构化输出。
4. `app/schemas/__init__.py`
   - 作为 schema 包聚合入口，减少后续各层导入路径碎片化。
   - 在项目规模变大后，这种统一出口会明显提高可维护性。

本轮完成标准：

- 文档、任务、分页、错误响应的 schema 固定
- 与进阶方案中的接口契约保持一致
- 能解释为什么企业项目先定 schema 再写接口实现

## 第 5 轮｜Repository 层骨架

目标：先把数据访问层拆出来，不让后续 service 直接堆 SQLAlchemy 查询。

本轮生成的 `.py` 文件：

1. `app/repositories/document_repository.py`
   - 封装文档的创建、查询、分页过滤、版本相关读取等数据库操作。
   - 让文档相关 SQL 逻辑不直接混在 service 里。
2. `app/repositories/chunk_repository.py`
   - 封装 chunk 的批量写入、按文档查询、重处理时删除旧 chunk 等操作。
   - 这里会承接后续最容易出性能和幂等问题的批处理逻辑。
3. `app/repositories/task_repository.py`
   - 封装任务创建、抢占、状态更新、心跳刷新、失败回写等任务表操作。
   - 它直接关系到 worker 模式是否稳定，是企业感很强的一层。
4. `app/repositories/event_repository.py`
   - 封装事件写入和时间线查询逻辑，保证事件记录不会散落在各层随手写。
   - 后续排障和任务回溯会大量依赖这里。
5. `app/repositories/__init__.py`
   - 作为 repository 包入口，统一导出仓储类或工厂。
   - 主要作用是让 service 层引用方式更稳定、更清晰。

本轮完成标准：

- 常见查询和写入职责下沉到 repository
- 为后续 service 层编排留干净接口
- 能说明 repository 在企业里主要解决什么问题

## 第 6 轮｜存储与文件解析基础

目标：先把“文件怎么保存、怎么读、怎么解析文本”这条链路拆成独立基础模块。

本轮生成的 `.py` 文件：

1. `app/storage/local_storage.py`
   - 封装本地文件存储的保存、读取、路径生成、删除等操作。
   - 第一版虽然用本地盘，但接口形态会按可替换对象存储的思路来写。
2. `app/storage/__init__.py`
   - 作为存储包入口，统一导出当前默认存储实现。
   - 后面切换 MinIO 或 S3 时可以尽量减少上层改动。
3. `app/utils/hashing.py`
   - 提供文件哈希计算逻辑，用于重复上传判断和幂等控制。
   - 这会直接影响“同文件重复上传”的企业化处理策略。
4. `app/utils/file_parser.py`
   - 封装 `.txt`、`.md`、`.pdf` 的基础文本抽取逻辑。
   - 业务层只关心“拿到文本”，不关心底层是哪个解析库。
5. `app/utils/text_splitter.py`
   - 封装文本清洗和 chunk 切分规则，输出带顺序和层级信息的分段结果。
   - 第一阶段就把分段做好，后续接 RAG 才不会推倒重来。
6. `app/utils/__init__.py`
   - 作为工具包入口，统一导出常用工具函数。
   - 主要目的是减少导入路径散乱，保持工程整洁。

本轮完成标准：

- 文件保存、哈希计算、基础文本解析、文本分段有独立职责
- 后续替换 MinIO/S3 或更强解析器时不需要改业务主链路

## 第 7 轮｜文档与任务 Service 主链路

目标：正式把上传、创建任务、状态变更、事件记录的业务编排串起来。

本轮生成的 `.py` 文件：

1. `app/services/document_service.py`
   - 编排文档上传主流程：校验、幂等判断、文件保存、文档创建、任务创建、事件写入。
   - 它是文档相关业务链路的总协调层，不负责底层 SQL 和文件细节。
2. `app/services/task_service.py`
   - 负责任务状态流转、任务详情查询、重处理入口规则校验等任务侧业务编排。
   - 后续 API 和 worker 都会依赖这里的状态更新逻辑。
3. `app/services/identity_service.py`
   - 负责把请求头里的身份透传数据转换为统一的业务身份对象。
   - 这样审计字段、日志字段、权限保留字段都能稳定从这里拿。
4. `app/services/__init__.py`
   - 作为 service 包聚合入口，统一导出核心服务类。
   - 在依赖注入和后续模块组装时会更清晰。

本轮完成标准：

- 上传文档时能完成身份提取、幂等判断、文档创建、任务创建、事件写入
- 文档和任务状态流转遵守进阶方案冻结规则
- 能讲清 service 层为什么是“业务编排层”

## 第 8 轮｜解析 Service 与 Worker 运行器

目标：把“任务执行”和“Web 请求处理”真正拆开，落实 worker 模式。

本轮生成的 `.py` 文件：

1. `app/services/parsing_service.py`
   - 编排单个解析任务的执行步骤：抽取文本、清洗、分段、写 chunk、回写状态。
   - 它关注“解析任务怎么完成”，不关心任务是被谁调度起来的。
2. `app/workers/document_parser_worker.py`
   - 负责单任务级别的 worker 执行逻辑，包括状态推进、异常处理、重试触发。
   - 可以理解为“真正干活的执行器”。
3. `app/workers/task_runner.py`
   - 负责轮询 `pending` 任务、抢占任务、启动解析、刷新心跳。
   - 它是“调度器”角色，不直接做文本解析细节。
4. `app/workers/__init__.py`
   - 作为 worker 包入口，统一组织 worker 相关模块。
   - 主要保证导入和运行入口结构稳定。

本轮完成标准：

- Worker 可轮询 `pending` 任务
- 抢占、心跳、重试、成功/失败回写具备最小闭环
- 能说明为什么这里比 FastAPI `BackgroundTasks` 更贴近企业

## 第 9 轮｜API 路由实现

目标：把文档、任务、重处理这些核心接口真正暴露出来。

本轮生成的 `.py` 文件：

1. `app/api/v1/documents.py`
   - 暴露文档上传、列表、详情、chunk 查询、归档、reprocess 等文档相关接口。
   - 它主要做参数接收、响应返回和调用 service，不直接写业务逻辑。
2. `app/api/v1/tasks.py`
   - 暴露任务详情、任务事件、基础状态查询等任务相关接口。
   - 后续如果 SSE 最终仍挂在这里，这个文件还会承接一部分流式接口能力。

本轮完成标准：

- 文档上传、列表、详情、chunks、归档、reprocess 可调用
- 任务详情、事件查询、基础状态查询可调用
- 错误响应结构遵守统一契约

## 第 10 轮｜缓存与任务状态加速层

目标：给热点查询和任务状态补 Redis 边界，但不让 Redis 变成事实来源。

本轮生成的 `.py` 文件：

1. `app/services/cache_service.py`
   - 封装文档列表缓存、任务状态缓存、缓存失效和降级策略。
   - 它的关键不是“多快”，而是把 Redis 严格放在加速层的位置上。

本轮完成标准：

- 任务状态和文档列表有最小缓存策略
- Redis 不可用时能优雅降级
- 能讲清 PostgreSQL 与 Redis 的职责边界

## 第 11 轮｜SSE 实时进度推送

目标：把长任务进度实时反馈补齐，形成更接近企业内部工具的体验。

本轮生成的 `.py` 文件：

1. `app/api/v1/task_stream.py`
   - 专门承接任务进度 SSE 输出，把事件格式、心跳、断连处理集中到一个地方。
   - 如果最终仍决定合并到 `tasks.py`，这一轮的本质仍然是补“流式反馈能力”。

说明：

- 若最终决定仍把 SSE 路由放在 `tasks.py`，这一轮就改为“重构 `tasks.py` 并补充 SSE 相关辅助逻辑”
- 这一轮的重点不是新增很多业务，而是补流式反馈能力

本轮完成标准：

- 客户端可订阅任务进度
- SSE 事件类型和数据结构遵守已冻结契约
- 断连不影响后台任务

## 第 12 轮｜异常处理、统一错误码与可观测性基线

目标：把企业里最常见的“能不能排障、能不能看懂失败原因”补齐。

本轮生成的 `.py` 文件：

1. `app/core/error_codes.py`
   - 统一定义业务错误码，避免后续接口和 service 各自发明错误标识。
   - 这是让错误响应、日志、文档三者能对齐的基础。
2. `app/core/exception_handlers.py`
   - 实现全局异常处理器，把项目异常统一转换成规范化 HTTP 响应。
   - 同时在这里把 request_id 等关键字段带回给客户端。

说明：

- 若第 1 轮里 `exceptions.py` 只放了基础异常定义，这一轮补统一 handler 和错误码枚举
- 若日志增强需要额外辅助模块，可在本轮扩到 `app/core/logging.py`

本轮完成标准：

- 常见错误码统一
- 全局异常处理成型
- `request_id / task_id / document_id / user_id` 这些关键字段能贯穿日志与错误响应

## 第 13 轮｜基础测试

目标：先覆盖最关键链路，而不是一开始追求全量测试。

本轮生成的 `.py` 文件：

1. `tests/conftest.py`
   - 放测试公共夹具、测试应用初始化、测试数据库或依赖替换逻辑。
   - 它是所有后续测试文件共享的基础设施层。
2. `tests/test_health.py`
   - 验证服务最小可用性和基础健康检查接口行为。
   - 这是最轻但最稳定的冒烟测试入口。
3. `tests/test_documents_api.py`
   - 覆盖文档上传、列表、详情、归档、reprocess 等主要文档接口。
   - 重点验证接口契约、状态变化和幂等行为。
4. `tests/test_tasks_api.py`
   - 覆盖任务详情、任务事件、任务状态查询等任务侧接口。
   - 重点验证任务查询链路和错误响应是否稳定。
5. `tests/test_worker_flow.py`
   - 验证 worker 侧主成功链路、失败链路、重试链路等核心流程。
   - 这是整个项目里最能体现“企业任务系统感”的测试文件之一。

本轮完成标准：

- 上传、任务创建、任务执行、失败回写、基础查询有测试覆盖
- 至少能验证一条主成功链路和一条主失败链路

## 第 14 轮｜RAG 前置增强字段与检索准备

目标：在不接 LLM 的前提下，把第一阶段后端补到“天然适合接 RAG”的状态。

本轮生成的 `.py` 文件：

1. `app/services/retrieval_prep_service.py`
   - 负责整理更适合检索的 chunk 元数据和过滤字段，为后续 embedding 和召回做前置清洗。
   - 它不是正式 RAG 服务，而是第一阶段到第二阶段之间的桥梁层。

说明：

- 这一轮不是正式做 RAG，而是把 chunk 元数据、过滤条件、检索前置准备进一步整理好

## 第 15 轮及之后｜第二阶段 RAG 扩展

进入这一阶段时，再单独细分新的代码生成计划。默认会按以下方向继续拆轮：

1. `EmbeddingService`
2. `VectorStore`
3. `RetrievalService`
4. `PromptBuilder`
5. `AnswerService`
6. RAG API 与流式问答

## 使用方式

后续如果你希望我继续生成代码，请直接用下面这种表达：

`接下来生成第1轮代码`

`接下来生成第2轮代码`

如果你没有这样说，而是继续问某个概念、某段代码、某个设计原因，我会默认你是在提问，不会自动进入下一轮代码生成。