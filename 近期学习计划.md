# 近期学习计划

最后更新时间：2026-07-24

## 当前执行主线

继续按 `代码生成计划.md` 分轮生成企业知识库文档处理后端。
后续生成代码时，注释和文档字符串默认使用中文。
每轮生成结束后，补充该轮代码的推荐阅读顺序。
每轮生成还要同步创建或更新独立 Markdown 错误案例分析文件，至少提供 2 个有代表性的逻辑级别或工程边界级别错误案例、企业工程师第一视角的排障流程、每一步输出演示、修复思路和正确输出样例，重点练习“看日志定位问题”的真实开发过程。

## 当前进度

- 第 1 轮：项目脚手架与运行入口，已完成
- 第 2 轮：数据库基础与 ORM 骨架，已完成
- 第 3 轮：核心数据模型，待开始
- 项目环境：已由 uv 管理 `.venv`、`pyproject.toml` 和 `uv.lock`

## 沉浸式调试要求

后续每轮代码生成都按以下顺序组织：

1. 先给出正确实现
2. 创建或更新独立 Markdown 错误案例分析文件，例如 `沉浸式错误案例分析-第n轮.md`
3. 至少给出 2 个有代表性的错误案例，不能使用简单语法错误、拼写错误、缺少 import、缩进错误等低级问题凑数
4. 错误案例至少应达到逻辑级别或工程边界级别，例如 ORM 元数据注册、Session 生命周期、事务边界、状态流转、一致性或并发保护问题
5. 每个案例都要给出执行命令、错误输出或日志输出、排障路径、定位模块、原因分析、修复方式、再次验证命令和正确输出样例
6. 对话回复可以摘要说明，完整过程必须保存在独立 Markdown 文件中，方便后续复习

## 代码阅读顺序

### 第 1 轮

建议阅读顺序：

1. `app/main.py`
2. `app/api/v1/health.py`
3. `app/api/v1/__init__.py`
4. `app/core/config.py`
5. `app/core/exceptions.py`
6. `app/core/auth_context.py`
7. `app/core/logging.py`
8. `app/core/enums.py`
9. `app/api/__init__.py`
10. `app/__init__.py`

阅读重点：

- 先看入口怎么装配路由和异常
- 再看健康检查这种最小接口怎么落地
- 然后看配置、异常、身份透传、日志这些基础设施如何协同
- 最后扫包入口，理解项目目录怎么组织

### 第 2 轮

建议阅读顺序：

1. `app/db/base.py`
2. `app/models/base.py`
3. `app/models/__init__.py`
4. `app/db/session.py`

阅读重点：

- 先看为什么全项目只能有一个统一的 SQLAlchemy `Base`
- 再看 ORM 通用字段如何从 `BaseModel`、`IdMixin`、`TimestampMixin` 复用
- 然后看模型包入口为什么要为 Alembic 和测试建表保留统一导入位置
- 最后看 `engine`、`SessionLocal`、`get_db_session()` 和 `create_session()` 如何分别服务 Web 请求与 worker 进程

配套调试文档：

- `沉浸式错误案例分析-第2轮.md`

环境验证命令：

```powershell
uv run python -B -c "from app.main import app; print(app.title)"
uv run python -B -c "from app.db.base import Base, import_all_models; import_all_models(); print(list(Base.metadata.tables.keys()))"
uv run python -B -c "from app.db.session import get_db_session; dep = get_db_session(); print(type(dep).__name__)"
uv run ruff check app
```

## 下一次任务

当用户说 `接下来生成第3轮代码` 时，开始生成第 3 轮代码。

第 3 轮目标：

- 定义 `Document` 文档主实体
- 定义 `DocumentChunk` 文档分段实体
- 定义 `ProcessingTask` 解析任务实体
- 定义 `DocumentEvent` 事件流水实体
- 把状态、版本、幂等、心跳、重试相关字段落到 ORM 模型层
- 继续同步附带错误案例、日志定位思路、修复流程和正确输出样例，帮助用户沉浸式理解核心业务表设计

## 本阶段学习提醒

- 第 2 轮只是数据库基础设施，不等于已经有业务表
- 第 3 轮会把文档、任务、事件、chunk 四个核心实体真正落到 ORM 层
- 先理解“表之间为什么这样拆”，再追求接口功能，会更接近企业项目的学习路径
