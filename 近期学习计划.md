# 近期学习计划

最后更新时间：2026-08-18

## 当前执行主线

继续按 `代码生成计划.md` 分轮生成企业知识库文档处理后端。

后续生成代码时：

- 注释和文档字符串默认使用中文
- 每轮生成结束后，补充该轮代码的推荐阅读顺序
- 每轮生成还要同步提供至少 2 个有代表性的错误案例、企业工程师第一视角的排障流程、修复思路和正确输出样例
- 错误案例必须落到独立 Markdown 文件中，方便复习和上下文重置后的衔接

## 当前进度

- 第 1 轮：项目脚手架与运行入口，已完成
- 第 2 轮：数据库基础与 ORM 骨架，已完成
- 第 3 轮：核心数据模型，已完成
- 第 4 轮：Pydantic Schema 与统一响应结构，已完成
- 第 5 轮：Repository 层骨架，待开始

## 当前文档入口

- [`README.md`](./README.md)
- [`代码生成计划.md`](./代码生成计划.md)
- [`学习进度追踪.md`](./学习进度追踪.md)
- [`沉浸式错误案例分析-第2轮.md`](./沉浸式错误案例分析-第2轮.md)
- [`沉浸式错误案例分析-第3轮.md`](./沉浸式错误案例分析-第3轮.md)
- [`沉浸式错误案例分析-第4轮.md`](./沉浸式错误案例分析-第4轮.md)

## 第 4 轮推荐阅读顺序

1. `app/schemas/common.py`
2. `app/schemas/document.py`
3. `app/models/document_chunk.py`
4. `app/schemas/task.py`
5. `app/models/document_event.py`
6. `app/schemas/__init__.py`
7. `沉浸式错误案例分析-第4轮.md`

阅读重点：

- 先看 `SchemaModel` 如何统一严格字段、ORM 属性读取、别名输出和 UTC 时间
- 再看文档上传元数据为什么不直接包含 `UploadFile`
- 对照 Chunk ORM 与 Schema，理解 Python 属性 `chunk_metadata` 如何转换成 API 字段 `metadata`
- 再看任务详情、事件时间线和 SSE 如何复用同一套枚举与进度约束
- 注意上传响应和 SSE 不只校验单字段，还会校验事件类型、状态和进度之间的一致性
- 最后看聚合入口如何为后续 route、service 和测试提供稳定导入位置

本轮验证命令：

```powershell
uv run ruff check app/schemas
uv run ruff format --check app/schemas
uv run python -B -c "from app.schemas import DocumentListQuery; print(DocumentListQuery.model_json_schema()['properties']['page_size'])"
```

正确输出应包含：

```text
All checks passed!
4 files already formatted
{'default': 20, 'maximum': 50, 'minimum': 1, 'title': 'Page Size', 'type': 'integer'}
```

## 第 5 轮目标

- 建立四类业务 Repository 和聚合入口
- 把文档、chunk、任务和事件的 SQLAlchemy 数据访问集中到对应模块
- 支持文档分页过滤、版本读取、chunk 批量写入、任务查询与后续抢占、事件时间线
- 保持 Repository 只负责数据访问，事务提交和状态机业务判断留给 Service

## 学习重点

- 已理解：`UtcDateTime = Annotated[datetime, AfterValidator(_ensure_utc)]` 定义了带 UTC 后置校验的可复用 Schema 类型，而非单纯的 `datetime` 别名
- 已理解：`Annotated` 不直接执行校验，它保留 `datetime` 作为基础类型，并把 `AfterValidator` 等元数据交给 Pydantic 处理
- 已理解：`__all__` 是模块的公开名称清单，约束 `from module import *` 的导入范围，但不禁止显式导入模块内部名称
- 已理解：`models` 是数据库持久化模型，`schemas` 是 API 数据契约；Schema 可从 ORM 对象读取属性，但不应直接暴露 ORM 的内部字段和关系
- 已理解：Python 3.12 的 `class SuccessResponse[ResponseDataT]` 在方括号中声明泛型类型参数，具体使用时由 `SuccessResponse[具体类型]` 决定 `data` 的类型
- 已理解：文件上传请求中的业务字段与文件流属于不同职责；Pydantic Schema 校验标题、标签等元数据，`UploadFile` 负责文件内容的读取、保存和关闭
- 已理解：SSE 适合文档解析等服务端持续产生进度的长任务；`TaskSseEvent` 约束事件类型与任务状态快照一致，实际推送路由在后续轮次实现
- 已理解：持久化的 `DocumentEvent` 与实时的 SSE 事件不是同一对象；SSE 校验器检查当前消息内部一致性，数据库状态与事件流水的一致性仍由后续 Service/事务设计保证
- 已理解：事件流水不是默认面向普通用户直接展示的数据，而是通过按创建时间升序分页的任务事件接口，为内部时间线、审计排障和 SSE 断线恢复提供历史来源
- 已理解：`AliasChoices` 只控制输入时允许的多个字段名；Schema 的 `metadata` 字段可以从 ORM 的 `chunk_metadata` 属性读取，但序列化时统一输出为 API 名称 `metadata`
- 已理解：Session 脏状态表示 ORM 已检测到对象存在待持久化修改；`commit()` 会据此决定是否执行 `UPDATE`
- 已理解：普通 JSON 的 `append()` 等原地修改可能不触发脏状态；模型层通过 `MutableList.as_mutable(JSON)` 显式解决这一持久化边界问题
- 为什么 API 请求 / 响应模型不能直接使用 ORM 模型
- 如何统一分页、成功响应和错误响应的外层壳
- 如何在 API 层继续使用 `chunk_metadata` / `event_metadata` 的 `metadata` 表达
- 如何复用枚举和公共字段，同时避免 Schema 之间相互耦合
- 下一步重点理解 Repository 为什么不应该自行决定完整业务事务是否提交

## README 后续补充方向

`README.md` 会随着代码继续补充，后续重点会增加：

- 文档上传接口说明
- 任务查询与 SSE 接口说明
- worker 运行方式
- 数据库迁移说明
- 测试命令与验证方式
- 错误案例与排障流程
