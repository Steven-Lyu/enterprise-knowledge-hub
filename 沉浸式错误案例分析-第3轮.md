# 沉浸式错误案例分析-第 3 轮

最后更新时间：2026-07-29

对应代码轮次：第 3 轮｜核心数据模型

本文件只分析当前已经生成的 ORM 模型，不提前借用 repository、service 或 worker
代码。两个案例都不是语法错误，而是能够正常启动、却会在真实数据流转中破坏业务规则的模型层逻辑错误。

第 3 轮正确模型需要保证：

- 同一文档最多只有一个 `pending` 或 `running` 活跃任务
- 已结束的历史任务可以保留，并允许后续创建重处理任务
- 文档标签、标题路径和扩展元数据的原地修改能够被 SQLAlchemy 检测并持久化
- 数据库中的状态值与 `app/core/enums.py` 定义的字符串值一致

## 错误案例 1：用普通唯一约束限制 document_id，导致重处理任务永远无法创建

错误类型：数据库约束粒度错误 / 任务历史与活跃任务边界错误

### 1. 错误背景

方案要求“同一文档同一时刻只允许一个活跃任务”。一个看似直接、实际错误的实现，是对
`processing_tasks.document_id` 建普通唯一索引：

```python
# app/models/processing_task.py 中的错误写法
class ProcessingTask(BaseModel):
    __tablename__ = "processing_tasks"
    __table_args__ = (
        Index(
            "uq_processing_tasks_document_id",
            "document_id",
            unique=True,
        ),
    )
```

这段模型可以导入，表也能正常创建。问题在于普通唯一索引表达的是：

```text
一个 document_id 在整张 processing_tasks 表中永远只能出现一次
```

业务真正需要的是：

```text
一个 document_id 可以有多条历史任务，但 pending/running 最多只能有一条
```

### 2. 第一步：确认模型和表都能正常加载

执行：

```powershell
uv run python -B -c "from app.db.base import Base, import_all_models; import_all_models(); print(sorted(Base.metadata.tables))"
```

可能看到：

```text
['document_chunks', 'document_events', 'documents', 'processing_tasks']
```

这里不会报错，所以仅做 import 冒烟测试发现不了这个逻辑问题。

### 3. 第二步：创建第一条任务

假设文档 `doc-001` 已存在，先插入第一条任务：

```python
first_task = ProcessingTask(
    document_id="doc-001",
    status=TaskStatus.PENDING,
)
session.add(first_task)
session.commit()
print(first_task.status.value)
```

输出：

```text
pending
```

将任务执行完成：

```python
first_task.status = TaskStatus.SUCCESS
session.commit()
print(first_task.status.value)
```

输出：

```text
success
```

此时已经没有活跃任务，按冻结的状态机规则，文档应该允许重新解析。

### 4. 第三步：创建重处理任务时出现错误

```python
reprocess_task = ProcessingTask(
    document_id="doc-001",
    status=TaskStatus.PENDING,
)
session.add(reprocess_task)
session.commit()
```

SQLite 复现时可能看到：

```text
sqlalchemy.exc.IntegrityError: (sqlite3.IntegrityError)
UNIQUE constraint failed: processing_tasks.document_id
[SQL: INSERT INTO processing_tasks (... document_id, status ...)
VALUES (... 'doc-001', 'pending' ...)]
```

PostgreSQL 中可能看到：

```text
psycopg.errors.UniqueViolation: duplicate key value violates unique constraint
"uq_processing_tasks_document_id"
DETAIL: Key (document_id)=(doc-001) already exists.
```

### 5. 企业工程师第一视角排障路径

第一步，看到 `UniqueViolation` 后，不要先删除历史任务。先确认业务上是否真的存在活跃任务：

```sql
SELECT id, document_id, status, created_at
FROM processing_tasks
WHERE document_id = 'doc-001'
ORDER BY created_at;
```

输出示例：

```text
id        | document_id | status  | created_at
----------+-------------+---------+--------------------------
task-001  | doc-001     | success | 2026-07-29 09:00:00+00
```

只有一条 `success` 历史任务，没有 `pending/running`，说明不是 service 重复创建活跃任务。

第二步，检查任务表索引：

```sql
SELECT indexname, indexdef
FROM pg_indexes
WHERE tablename = 'processing_tasks';
```

错误输出中的关键内容：

```text
uq_processing_tasks_document_id |
CREATE UNIQUE INDEX uq_processing_tasks_document_id
ON public.processing_tasks USING btree (document_id)
```

索引没有 `WHERE` 条件，因此它错误地覆盖了所有历史状态。

第三步，回到定位模块：

```text
app/models/processing_task.py
```

确认问题属于模型约束，而不是任务创建流程、事务提交或 worker 抢占逻辑。

### 6. 根因分析

“同一时刻唯一”不是普通唯一约束，它是带状态条件的唯一性：

- `pending`、`running` 是活跃状态，需要参与唯一性判断
- `success`、`failed`、`cancelled` 是历史状态，不应该阻塞后续任务
- 删除历史任务来绕过约束会损失重试、排障和审计数据

因此，普通 `UNIQUE(document_id)` 的约束范围比业务规则大得多。

### 7. 修复方式

第 3 轮正确代码使用条件唯一索引：

```python
from sqlalchemy import Index, text


Index(
    "uq_processing_tasks_one_active_per_document",
    "document_id",
    unique=True,
    postgresql_where=text("status IN ('pending', 'running')"),
    sqlite_where=text("status IN ('pending', 'running')"),
)
```

这里同时声明 PostgreSQL 主目标和 SQLite 本地兜底的条件，生产与本地验证具有一致语义。

### 8. 修复后逐步验证

先创建并完成第一条任务：

```text
first_task=pending
first_task_after_finish=success
```

再创建重处理任务：

```text
reprocess_task=pending
```

这次提交成功，因为旧任务已经不是活跃状态。

不结束 `reprocess_task`，再创建第三条 `pending` 任务，正确结果应为：

```text
sqlalchemy.exc.IntegrityError: UNIQUE constraint failed:
processing_tasks.document_id
```

最终验证结论：

```text
历史任务可共存：通过
同一文档两个活跃任务：被数据库拒绝
```

这才准确实现了冻结规则，而不是简单地让一个文档只能有一条任务。

## 错误案例 2：JSON 字段使用普通 JSON，标签原地修改后静默丢失

错误类型：ORM 变更追踪逻辑错误 / JSON 持久化一致性错误

### 1. 错误背景

`Document.tags`、`DocumentChunk.heading_path`、chunk/event 的扩展元数据都是 JSON
容器。一个常见写法是直接映射普通 `JSON`：

```python
# app/models/document.py 中的错误写法
tags: Mapped[list[str]] = mapped_column(
    JSON,
    default=list,
    nullable=False,
)
```

插入整份列表没有问题，整体重新赋值也能保存：

```python
document.tags = ["制度", "人事"]
```

但企业代码更常见的是原地修改：

```python
document.tags.append("入职")
```

普通 `JSON` 类型默认不知道 Python 列表内部发生了变化，因此提交可能成功、数据却没有更新。

### 2. 第一步：创建初始文档

```python
document = Document(
    # 省略其他必填字段
    tags=["制度"],
)
session.add(document)
session.commit()
print(document.tags)
```

输出：

```text
['制度']
```

如果 SQLAlchemy 开启 `echo=True`，可以看到首次写入：

```text
INSERT INTO documents (..., tags, ...)
VALUES (..., '["制度"]', ...)
COMMIT
```

### 3. 第二步：原地追加标签并检查脏状态

```python
document.tags.append("入职")
print(document.tags)
print(session.is_modified(document, include_collections=True))
```

内存中的输出：

```text
['制度', '入职']
False
```

第一行让人误以为修改已经成功，第二行才暴露关键问题：Session 没有把文档识别为待更新对象。

### 4. 第三步：提交看似成功，但没有 UPDATE

```python
session.commit()
```

日志可能只有：

```text
COMMIT
```

本应出现的 SQL 没有出现：

```text
UPDATE documents SET tags=... WHERE documents.id=...
```

提交没有抛异常，因此这是典型的“静默数据丢失”。

### 5. 第四步：重新加载后修改消失

```python
session.expire(document, ["tags"])
print(document.tags)
```

输出：

```text
['制度']
```

刚刚追加的 `入职` 已消失。相同问题也会发生在：

- `DocumentChunk.heading_path.append(...)`
- `DocumentChunk.chunk_metadata["parser"] = "pypdf"`
- `DocumentEvent.event_metadata["retry_count"] = 1`

### 6. 企业工程师第一视角排障路径

第一步，先确认接口或业务代码执行后是否真的发出了 `UPDATE`，而不是只看 Python 对象：

```text
BEGIN
SELECT ... FROM documents WHERE documents.id = ?
COMMIT
```

如果没有 `UPDATE documents`，问题发生在 ORM 脏数据检测之前，不是数据库覆盖了数据。

第二步，在提交前打印：

```python
print(session.dirty)
print(session.is_modified(document, include_collections=True))
```

错误输出：

```text
IdentitySet([])
False
```

第三步，定位模型字段：

```powershell
rg "mapped_column\(JSON|as_mutable" app\models
```

错误实现只能找到 `mapped_column(JSON`，找不到 `MutableList.as_mutable` 或
`MutableDict.as_mutable`。

定位模块为：

```text
app/models/document.py
app/models/document_chunk.py
app/models/document_event.py
```

### 7. 根因分析

SQLAlchemy 能检测“属性被重新赋值”：

```python
document.tags = [*document.tags, "入职"]
```

但普通 JSON 容器内部的 `append`、`pop`、字典键赋值不会触发 ORM 属性赋值事件。
因此数据库、事务和 `commit()` 都没有故障，真正缺失的是容器级变更通知。

这个问题在企业系统中风险较高，因为：

- API 返回的当前内存对象可能显示新值
- 日志显示事务提交成功
- 重新请求或进程重启后数据才恢复成旧值
- 问题容易被误判为缓存覆盖或并发写入

### 8. 修复方式

列表字段使用 `MutableList`：

```python
from sqlalchemy import JSON
from sqlalchemy.ext.mutable import MutableList


tags: Mapped[list[str]] = mapped_column(
    MutableList.as_mutable(JSON),
    default=list,
    nullable=False,
)
```

字典字段使用 `MutableDict`：

```python
from sqlalchemy.ext.mutable import MutableDict


event_metadata: Mapped[dict[str, object]] = mapped_column(
    "metadata",
    MutableDict.as_mutable(JSON),
    default=dict,
    nullable=False,
)
```

Python 属性使用 `event_metadata`，数据库列仍叫 `metadata`。这是因为 `metadata`
是 SQLAlchemy 声明式模型的保留属性，不能直接作为 ORM 模型属性名。

### 9. 修复后逐步验证

再次原地追加：

```python
document.tags.append("入职")
print(session.is_modified(document, include_collections=True))
session.commit()
```

正确输出：

```text
True
```

SQL 日志中应出现：

```text
UPDATE documents SET tags='["制度", "入职"]'
WHERE documents.id = 'doc-001'
COMMIT
```

让当前属性过期并从数据库重读：

```python
session.expire(document, ["tags"])
print(document.tags)
```

正确输出：

```text
['制度', '入职']
```

最终验证结论：

```text
json_change_detected=True
persisted_tags=['制度', '入职']
```

## 第 3 轮排障结论

这一轮最需要建立的判断是：模型能 import、表能创建，只能证明 ORM 语法和基础映射成立，不能证明业务约束正确。

企业工程师还需要继续验证：

- 约束是否只覆盖它真正应该覆盖的状态和数据范围
- 历史记录是否会被错误约束阻塞
- JSON 容器的原地修改是否真的产生 `UPDATE`
- 枚举在数据库中存的是小写业务值，而不是 Python 成员名
- ORM 属性名与数据库列名不同时，Schema 层是否能保持稳定契约
