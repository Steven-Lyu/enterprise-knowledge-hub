# 项目初步方案

## 1. 项目定位

项目名称：`Enterprise Knowledge Hub Backend`

这是一个企业内部知识库文档处理后端，定位不是面向普通用户的聊天 demo，而是企业内部文档从“上传、归档、解析、分段、状态追踪、检索准备”到后续接入 RAG 的基础平台模块。

它服务的不是单次展示，而是真实企业中可能长期存在的知识沉淀与文档处理需求。项目设计目标是尽量接近企业内部工具团队或 AI 应用团队会维护的后端模块形态。

## 2. 项目背景

中小型企业或业务团队通常有大量分散文档，例如：

- 制度文件
- 产品说明
- 客服 FAQ
- 技术 SOP
- 报销流程
- IT 支持手册
- 项目复盘文档

这些资料通常散落在飞书、企业微信、网盘、邮件附件和本地文件夹中，会带来几个典型问题：

- 员工不知道最新制度和流程放在哪里
- 同一主题的资料存在多个版本，更新不可追踪
- 客服、实施、运营等岗位重复查询同类问题
- 新员工培训依赖人工带教，知识难沉淀
- AI 团队后续想接 RAG 时，原始文档无法直接用于检索和问答

这个系统解决的是：`如何把企业内部非结构化文档沉淀成可管理、可追踪、可解析、可检索、未来可问答的数据资产`。

## 3. 目标用户与业务场景

### 3.1 目标用户

| 用户角色 | 典型动作 | 价值 |
|---|---|---|
| 运营 / HR / 行政 | 上传制度、流程、FAQ 文档 | 统一沉淀企业知识 |
| 客服 / 售前 / 实施 | 查询产品说明、处理流程、常见问题 | 减少重复查资料 |
| 技术 / 内部工具团队 | 管理解析任务、排查失败文档 | 提供可维护的知识库底座 |
| AI 应用团队 | 后续接入 RAG 检索和问答 | 让文档具备 AI 化能力 |

### 3.2 核心业务场景

| 场景 | 说明 |
|---|---|
| 新员工入职 | HR 上传入职流程、报销制度、请假制度，新员工后续可检索 |
| 客服知识库 | 运营上传产品 FAQ、服务规则、升级处理流程，客服可快速查标准答案 |
| 内部 SOP 管理 | 技术或实施团队上传部署手册、故障处理流程、版本发布规范 |
| RAG 前置处理 | AI 团队把已解析分段的文档接入 embedding 和向量库 |

## 4. 第一阶段范围

两周纯后端版只做文档处理和知识库基础设施，不接 LLM。

### 4.1 必须实现

| 模块 | 目标 |
|---|---|
| 文档上传 | 支持 `.txt`、`.md`、`.pdf` 文件上传 |
| 元数据管理 | 记录部门、分类、标签、创建人、版本、可见范围 |
| 文件存储 | 第一版使用本地文件存储，封装成 `StorageService` |
| 数据库存储 | PostgreSQL 存文档、chunk、任务、事件日志 |
| 异步解析任务 | 上传后创建任务，后台执行解析 |
| 状态流转 | 文档和任务都有明确状态 |
| 文档分段 | 抽取文本后切成 chunk，供后续 RAG 使用 |
| 任务查询 | 能查询任务状态、进度、当前步骤、失败原因 |
| SSE 推送 | 能实时推送解析进度 |
| Redis 缓存 | 缓存任务状态、文档列表热点查询 |
| 异常处理 | 文件类型错误、解析失败、重复上传、Redis 不可用等场景要有处理 |
| 测试 | 至少覆盖上传、解析、查询、失败、缓存降级等关键链路 |

### 4.2 暂不实现

| 不做 | 原因 |
|---|---|
| 登录注册系统 | 当前重点是后端业务链路，不把时间消耗在权限系统 |
| 多租户 SaaS | 当前是企业内部系统，不做复杂平台化 |
| 前端页面 | 先用 API 文档和测试工具验证 |
| LLM 问答 | 第二阶段 RAG 再接 |
| Celery / RQ | 第一版先用轻量后台任务和任务表理解链路，后续可替换 |

## 5. 技术栈

| 层级 | 技术 | 说明 |
|---|---|---|
| Web 框架 | FastAPI | 主后端框架 |
| 数据模型 | Pydantic v2 | 请求、响应、配置校验 |
| ORM | SQLAlchemy 2.x | 企业项目常见 ORM |
| 数据库 | PostgreSQL | 主业务数据持久化 |
| 迁移 | Alembic | 管理表结构变化 |
| 缓存 | Redis | 任务状态、热点查询缓存 |
| 文件解析 | pypdf + Python 文本处理 | 第一版 PDF 解析保持简单 |
| 测试 | pytest + httpx | 接口测试和服务测试 |
| 包管理 | uv | 与当前学习路线一致 |
| 代码质量 | ruff | 保持基本工程规范 |

说明：

- 第一版优先按 PostgreSQL 设计
- 如果本地环境暂时不便，也可以保留 SQLite 作为开发兜底，但项目结构仍以 PostgreSQL 为主
- Redis 是加速层，不是主存储

## 6. 系统架构

### 6.1 总体架构

```text
Client / API Tester
        |
FastAPI API Layer
        |
Service Layer
        |
Repository Layer
        |
PostgreSQL
        |
Redis
        |
Local File Storage
```

### 6.2 项目目录建议

```text
enterprise_knowledge_hub/
  app/
    api/
      v1/
        documents.py
        tasks.py
        health.py
    core/
      config.py
      logging.py
      exceptions.py
      enums.py
    db/
      session.py
      base.py
    models/
      document.py
      document_chunk.py
      processing_task.py
      document_event.py
    schemas/
      document.py
      task.py
      common.py
    repositories/
      document_repository.py
      chunk_repository.py
      task_repository.py
      event_repository.py
    services/
      document_service.py
      parsing_service.py
      task_service.py
      cache_service.py
    workers/
      document_parser_worker.py
    storage/
      local_storage.py
    utils/
      hashing.py
      text_splitter.py
      file_parser.py
  migrations/
  sample_docs/
  tests/
  pyproject.toml
  README.md
  .env.example
```

### 6.3 模块职责

| 模块 | 职责 |
|---|---|
| `api` | 暴露 HTTP / SSE 接口，处理请求参数和响应 |
| `schemas` | Pydantic 请求响应模型 |
| `services` | 业务流程编排，如上传、创建任务、解析、分段、状态更新 |
| `repositories` | 数据访问，隔离 SQLAlchemy 查询 |
| `models` | SQLAlchemy ORM 表结构 |
| `db` | PostgreSQL session、事务管理 |
| `cache` | Redis 客户端、任务状态缓存、热点查询缓存 |
| `storage` | 文件保存与读取，第一版用本地目录，后续可替换 MinIO/S3 |
| `workers` | 文档解析任务执行逻辑，第一版用轻量后台任务 |
| `core` | 配置、日志、异常、枚举、通用响应 |

## 7. 核心数据模型

### 7.1 Document

| 字段 | 说明 |
|---|---|
| `id` | 文档 ID |
| `title` | 文档标题 |
| `filename` | 原始文件名 |
| `file_type` | `txt` / `md` / `pdf` |
| `file_size` | 文件大小 |
| `file_hash` | 文件哈希，用于重复上传判断 |
| `storage_path` | 本地存储路径 |
| `department` | 所属部门 |
| `category` | 文档分类 |
| `tags` | 标签列表 |
| `visibility` | 可见范围 |
| `version` | 文档版本 |
| `status` | 文档状态 |
| `created_by` | 上传人 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

### 7.2 DocumentChunk

| 字段 | 说明 |
|---|---|
| `id` | chunk ID |
| `document_id` | 所属文档 |
| `chunk_index` | 第几个分段 |
| `section_title` | 所属章节标题 |
| `content` | 分段内容 |
| `char_count` | 字符数 |
| `metadata` | 扩展元数据 |
| `created_at` | 创建时间 |

### 7.3 ProcessingTask

| 字段 | 说明 |
|---|---|
| `id` | 任务 ID |
| `document_id` | 对应文档 |
| `task_type` | 如 `parse_document` |
| `status` | 任务状态 |
| `progress` | 0-100 |
| `current_step` | 当前步骤 |
| `error_message` | 失败原因 |
| `started_at` | 开始时间 |
| `finished_at` | 结束时间 |

### 7.4 DocumentEvent

| 字段 | 说明 |
|---|---|
| `id` | 事件 ID |
| `document_id` | 对应文档 |
| `task_id` | 对应任务 |
| `event_type` | 事件类型 |
| `message` | 事件描述 |
| `metadata` | 扩展信息 |
| `created_at` | 创建时间 |

### 7.5 状态定义

| 类型 | 状态 |
|---|---|
| 文档状态 | `uploaded`、`queued`、`processing`、`parsed`、`failed`、`archived` |
| 任务状态 | `pending`、`running`、`success`、`failed`、`cancelled` |
| 解析步骤 | `save_file`、`extract_text`、`clean_text`、`split_chunks`、`persist_chunks`、`complete` |

### 7.6 为什么这些模型像企业项目

| 设计点 | 企业意义 |
|---|---|
| `file_hash` | 支持重复上传检测和幂等 |
| `department/category/tags` | 企业知识按组织和业务分类 |
| `status/progress/current_step` | 长任务可追踪，方便排障 |
| `DocumentEvent` | 记录处理过程，便于定位问题 |
| `version` | 文档在企业内部会持续更新，不是一次性 demo 数据 |

## 8. API 设计

### 8.1 第一阶段 API

| Method | Path | 用途 |
|---|---|---|
| `POST` | `/api/v1/documents` | 上传文档并创建解析任务 |
| `GET` | `/api/v1/documents` | 分页查询文档，支持状态、部门、分类、关键词过滤 |
| `GET` | `/api/v1/documents/{document_id}` | 查询文档详情 |
| `DELETE` | `/api/v1/documents/{document_id}` | 归档文档 |
| `GET` | `/api/v1/documents/{document_id}/chunks` | 查询文档分段 |
| `POST` | `/api/v1/documents/{document_id}/reprocess` | 重新触发解析任务 |
| `GET` | `/api/v1/tasks/{task_id}` | 查询任务状态 |
| `GET` | `/api/v1/tasks/{task_id}/events` | 查询任务事件 |
| `GET` | `/api/v1/tasks/{task_id}/stream` | SSE 推送任务进度 |
| `GET` | `/api/v1/health` | 健康检查 |

### 8.2 上传接口字段

```text
file: UploadFile
title: str
department: str
category: str
tags: list[str]
visibility: str
created_by: str
```

### 8.3 上传成功响应示例

```json
{
  "document_id": "doc_001",
  "task_id": "task_001",
  "status": "queued",
  "message": "document uploaded and parsing task created"
}
```

### 8.4 SSE 事件示例

```text
event: progress
data: {"task_id":"task_001","status":"running","progress":60,"current_step":"split_chunks"}
```

## 9. 核心业务流程

### 9.1 上传流程

```text
接收文件
-> 校验文件类型和大小
-> 计算 file_hash
-> 判断是否重复
-> 保存文件到本地 storage
-> 写入 Document
-> 创建 ProcessingTask
-> 写入 DocumentEvent
-> 写入 Redis 初始任务状态
-> 启动后台解析任务
-> 返回 document_id 和 task_id
```

### 9.2 解析流程

```text
读取原始文件
-> 根据文件类型抽取文本
-> 清洗文本
-> 按标题和长度切分 chunk
-> 删除旧 chunk
-> 写入新 chunk
-> 更新 Document 状态为 parsed
-> 更新 ProcessingTask 为 success
-> 写入 DocumentEvent
-> Redis 更新最终状态
```

### 9.3 失败流程

```text
解析过程中抛错
-> 捕获异常
-> Document 标记为 failed
-> ProcessingTask 标记为 failed
-> 写入 error_message
-> 写入 DocumentEvent
-> Redis 更新失败状态
-> API 查询时可看到失败原因
```

### 9.4 查询流程

```text
查询文档列表
-> 构造过滤条件
-> 尝试读取 Redis 缓存
-> 未命中则查询 PostgreSQL
-> 写入短 TTL 缓存
-> 返回分页结果
```

### 9.5 关键失败处理

| 失败点 | 处理 |
|---|---|
| 文件类型不支持 | 返回 `400`，不创建任务 |
| 文件重复上传 | 第一版默认返回已有文档信息 |
| 解析失败 | 文档状态改为 `failed`，任务状态改为 `failed`，记录失败原因 |
| Redis 不可用 | 不阻塞主流程，降级到 PostgreSQL |
| SSE 断开 | 不影响后台任务，客户端可重新查询任务状态 |

## 10. 样例数据方案

数据不使用真实公司内部资料，避免隐私和版权问题。项目内准备一套“模拟企业内部资料包”，内容按真实企业文档风格编写。

建议第一批文档数量：`20-30` 份，后续扩到 `60+`。

### 10.1 文档分类

| 分类 | 文件示例 |
|---|---|
| HR 制度 | 入职流程、试用期说明、请假制度、差旅报销规范 |
| IT 支持 | VPN 使用手册、账号开通流程、密码重置流程 |
| 客服 SOP | 客诉升级流程、退款处理规则、工单优先级说明 |
| 产品知识 | 套餐差异说明、功能 FAQ、客户开通流程 |
| 实施交付 | 上线检查清单、客户培训流程、部署前准备 |
| 安全规范 | 数据分级规范、敏感信息外发审批 |
| 研发流程 | 发布流程、故障复盘模板、接口变更通知 |

### 10.2 样例文件目录

```text
sample_docs/
  hr/
    onboarding_policy.md
    reimbursement_policy.md
  it/
    vpn_user_guide.md
    account_request_process.txt
  support/
    refund_sop.md
    ticket_priority_policy.md
  product/
    pricing_faq.md
  security/
    data_classification_policy.md
```

第一版以 `.md` 和 `.txt` 为主，再补 `1-2` 个 `.pdf` 用于测试 PDF 解析链路。

## 11. 为什么它不像 demo

关键区别不在功能数量，而在系统边界和结构设计。

| demo 项目 | 企业项目型设计 |
|---|---|
| 上传文件后直接返回文本 | 上传后创建任务，后台解析，状态可追踪 |
| 只有一个 document 表 | 有 Document、Chunk、Task、Event 多个核心实体 |
| 没有状态机 | 文档和任务都有明确状态流转 |
| 没有错误追踪 | 失败原因进入 task 和 event |
| 没有缓存边界 | Redis 只做任务状态和热点查询，不当主存储 |
| 没有后续扩展 | DocumentChunk 直接作为 RAG 输入 |
| 所有逻辑写在路由里 | 分成 api、service、repository、worker、storage |

## 12. 两周执行节奏

### 第 1-2 天：项目骨架

| 目标 | 内容 |
|---|---|
| 工程初始化 | `pyproject.toml`、FastAPI app、配置、日志 |
| 数据库基础 | SQLAlchemy、Alembic、基础模型 |
| 健康检查 | `/api/v1/health` |
| 文档结构 | README 写清背景和启动方式 |

### 第 3-5 天：文档上传与数据库

| 目标 | 内容 |
|---|---|
| 上传接口 | 校验文件、保存本地、计算 hash |
| 文档表 | 写入 Document |
| 任务表 | 创建 ProcessingTask |
| 查询接口 | 文档列表、详情、归档 |

### 第 6-8 天：解析与分段

| 目标 | 内容 |
|---|---|
| 解析服务 | 支持 txt、md、pdf |
| 文本清洗 | 去空行、规范换行 |
| 分段服务 | 按标题和长度切 chunk |
| chunk 入库 | 写入 DocumentChunk |
| 事件日志 | 写入 DocumentEvent |

### 第 9-11 天：任务状态、Redis、SSE

| 目标 | 内容 |
|---|---|
| 任务状态 | `pending`、`running`、`success`、`failed` |
| Redis 缓存 | 任务进度、文档列表缓存 |
| SSE | 实时推送解析进度 |
| 降级 | Redis 不可用时不影响主流程 |

### 第 12-14 天：测试、文档、复盘

| 目标 | 内容 |
|---|---|
| 测试 | 上传、解析、失败、查询、SSE |
| 样例数据 | `20-30` 份模拟企业文档 |
| README | 项目背景、架构、接口、启动方式 |
| 复盘材料 | 能讲清完整链路和设计取舍 |

## 13. 验收标准

### 13.1 你应该能回答的问题

- 为什么企业文档处理要设计成异步任务？
- 为什么文档状态和任务状态要分开？
- 为什么 Redis 不能替代 PostgreSQL？
- 为什么要有 DocumentChunk，而不是只存完整文档？
- 为什么要记录 DocumentEvent？
- 文件重复上传怎么处理？
- PDF 解析失败怎么排查？
- SSE 断开会不会影响后台任务？
- 这个项目如何升级成 RAG？
- 这个项目和普通 CRUD demo 的区别在哪里？

### 13.2 技术验收

| 项目 | 标准 |
|---|---|
| 启动 | FastAPI 服务能正常启动 |
| 上传 | 能上传 `.txt`、`.md`、`.pdf` |
| 解析 | 能生成 chunk |
| 查询 | 能查文档、任务、事件、chunk |
| SSE | 能看到解析进度 |
| 缓存 | Redis 可用时走缓存，不可用时降级 |
| 异常 | 文件非法、解析失败、重复文件都有明确响应 |
| 测试 | 核心链路有 pytest 覆盖 |

## 14. 后续 RAG 扩展

第一阶段结束后，直接在当前项目上扩展，不重新开项目。

### 14.1 新增模块

| 模块 | 作用 |
|---|---|
| `EmbeddingService` | 对 chunk 生成 embedding |
| `VectorStore` | 存储和检索向量 |
| `RetrievalService` | top-k 召回 |
| `PromptBuilder` | 拼接上下文 |
| `AnswerService` | 调用 LLM 生成答案 |
| `CitationService` | 返回答案引用来源 |

### 14.2 新增 API

| Method | Path | 用途 |
|---|---|---|
| `POST` | `/api/v1/search` | 关键词或向量检索 |
| `POST` | `/api/v1/qa` | 基于知识库问答 |
| `GET` | `/api/v1/qa/{request_id}/stream` | 流式返回答案 |

### 14.3 第一阶段设计在 RAG 中的用途

| 第一阶段设计 | RAG 阶段用途 |
|---|---|
| Document | 知识源 |
| DocumentChunk | embedding 输入 |
| metadata | 过滤召回范围 |
| task | 复用为 embedding 任务 |
| event | 追踪建库过程 |
| SSE | 推送问答或建库进度 |

## 15. 实现原则

- 先保证链路完整，再追求功能丰富
- 业务逻辑放 service，不塞进 route
- 数据访问放 repository，不让 service 到处写查询
- Redis 是加速层，不是事实来源
- 文件存储要封装，不把路径逻辑散落在业务代码里
- 每个状态变化都要能被查询和解释
- README 不只写启动命令，还要写项目背景、架构和设计取舍

## 16. 总结

这个项目的价值不在于“做了一个上传文档功能”，而在于你能通过它理解一个企业内部知识库后端为什么需要：

- 明确的数据模型
- 可追踪的任务状态
- 可解释的异常处理
- 缓存与主存储的职责边界
- 为后续 RAG 保留的结构化基础

这也是它和普通课程 demo 的本质差别。
