# 学习进度追踪

最后更新时间：2026-07-24

## 当前主线

- 当前项目：`Enterprise Knowledge Hub Backend`
- 当前阶段：企业知识库文档处理后端第一阶段
- 当前目标：先完成后端工程底座，再逐步进入文档上传、异步解析、chunk 分段、Redis、SSE 和测试

## 已完成内容

### 2026-07-22

已完成第 1 轮代码生成：项目脚手架与运行入口。

同日根据用户反馈补充新的代码协作规则：

- 代码注释和文档字符串默认使用中文，不使用英文注释
- 每轮代码生成完成后，需要给出推荐阅读顺序
- 每轮代码生成完成后，推荐阅读顺序要写入 `近期学习计划.md`

本轮生成内容：

- `app/main.py`：FastAPI 应用入口，负责创建应用、注册 v1 路由、挂载生命周期和全局异常处理
- `app/core/config.py`：集中读取运行配置，包括数据库、Redis、存储目录、上传限制、文件类型等
- `app/core/logging.py`：统一日志初始化入口
- `app/core/enums.py`：集中定义文档状态、任务状态、解析步骤、可见范围、事件类型等枚举
- `app/core/exceptions.py`：定义项目级异常和最小统一错误响应处理
- `app/core/auth_context.py`：从请求头提取企业网关式身份透传信息
- `app/api/v1/health.py`：提供 `/api/v1/health` 健康检查接口
- `app/api/v1/__init__.py`、`app/api/__init__.py`、`app/__init__.py`：建立稳定包结构和 v1 路由聚合入口

验证结果：

- 已通过 Python 语法编译检查
- 当前 Codex 自带 Python 运行时缺少 `fastapi`，所以未能完成实际应用导入和服务启动验证

### 2026-07-24

已根据用户新的学习诉求补充沉浸式调试协作规则，并同步到 `Agents.md`：

- 每轮代码生成除了给出正确实现，还要附带至少 2 个有代表性的错误案例
- 每个错误案例都要说明运行后会看到什么报错或异常表现
- 需要从企业工程师第一视角给出排障路径：先看日志哪一段，再定位到哪个模块，分析可能原因，最后给出修复方式
- 最终还要补充正确输出样例，方便用户对照理解
- 错误案例应优先聚焦架构和逻辑层面，而不是刻意制造低级语法错误

已完成第 2 轮代码生成：数据库基础与 ORM 骨架。

本轮生成内容：

- `app/db/base.py`：定义全项目唯一的 SQLAlchemy `Base`，并提供 `import_all_models()` 作为 Alembic、测试建表和脚本初始化的模型聚合入口
- `app/db/session.py`：基于 `get_settings().database_url` 创建数据库 engine、`SessionLocal`、FastAPI 依赖 `get_db_session()`，并为 worker 提供 `create_session()`
- `app/models/__init__.py`：建立 ORM 模型包聚合入口，当前先导出通用基础模型和 mixin，后续第 3 轮继续聚合业务模型
- `app/models/base.py`：定义 `BaseModel`、`IdMixin`、`TimestampMixin` 和 `utc_now()`，统一主键、创建时间、更新时间等 ORM 通用字段

验证结果：

- 已使用 Codex 工作区 Python 对 `app` 下 14 个 `.py` 文件执行无缓存语法解析检查，全部通过
- 当前 PowerShell 环境没有可直接调用的 `python` 命令，已改用 Codex 工作区 Python 路径验证
- `compileall` 因当前沙箱不允许写入 `__pycache__` 失败，失败点不是代码语法
- 数据库模块导入验证因当前运行时缺少 `sqlalchemy` 失败，后续安装项目依赖后可继续做真实导入和连接验证

已根据用户反馈进一步修正沉浸式错误案例标准：

- 错误案例不能只写在对话回复中，必须创建或更新独立 Markdown 文件
- 错误案例分析必须包含每一步命令、错误输出或日志输出、定位路径、原因分析、修复方式和正确输出
- 错误案例不能用简单语法错误、拼写错误、缺少 import、缩进错误等低级问题凑数
- 错误案例至少要达到逻辑级别或工程边界级别，例如 ORM 元数据注册、Session 生命周期、事务边界、状态流转、一致性或并发保护问题

已创建第 2 轮独立错误案例分析文件：

- `沉浸式错误案例分析-第2轮.md`

已完成项目专用 Python 环境整理：

- 用户已通过 uv 创建 `.venv`
- 当前 `.venv` Python 版本：`3.12.13`
- 已确认 uv 可执行文件位置：`C:\Users\Lenovo\AppData\Local\Microsoft\WinGet\Links\uv.exe`
- 已使用 `uv init --bare --python 3.12` 创建 `pyproject.toml`
- 已通过 `uv sync` 同步运行依赖
- 已通过 `uv add --dev pytest httpx ruff` 添加开发依赖
- 已生成 `uv.lock`

环境验证结果：

- `uv run python -B -c "from app.main import app; print(app.title)"` 输出 `Enterprise Knowledge Hub Backend`
- `uv run python -B -c "from app.db.base import Base, import_all_models; import_all_models(); print(list(Base.metadata.tables.keys()))"` 输出 `[]`，符合第 2 轮尚未生成业务表的预期
- `uv run python -B -c "from app.db.session import get_db_session; dep = get_db_session(); print(type(dep).__name__)"` 输出 `generator`
- `uv run ruff check app` 已通过，输出 `All checks passed!`

## 当前状态

第 1 轮和第 2 轮已完成。项目已经具备基础包结构、应用入口、配置、日志、异常、枚举、身份上下文、健康检查路由、SQLAlchemy Base、数据库 Session 工厂、FastAPI Session 依赖和 ORM 通用基础模型。
当前第 1 轮代码中的英文注释和文档字符串也已统一改为中文，并已同步写入 `Agents.md`。
当前协作方式已经升级为“正确实现 + 独立错误案例 Markdown + 每一步输出演示 + 排障流程 + 修复验证 + 正确输出样例”的沉浸式学习模式，后续每轮代码都会按这个标准推进。
当前项目已经具备 uv 管理的专用虚拟环境、`pyproject.toml`、`uv.lock` 和基础依赖集合。

## 下一步

进入第 3 轮：核心数据模型。

重点文件：

- `app/models/document.py`
- `app/models/document_chunk.py`
- `app/models/processing_task.py`
- `app/models/document_event.py`

学习重点：

- 为什么文档、chunk、任务、事件要拆成四张核心表
- 为什么文档状态和任务状态不能混在一个字段里
- 为什么任务表需要进度、心跳、重试次数和 worker 标识
- 为什么事件表是企业排障和审计的关键基础
