# 学习进度追踪

最后更新时间：2026-08-18

## 当前主线

- 当前项目：`Enterprise Knowledge Hub Backend`
- 当前阶段：企业知识库文档处理后端第一阶段
- 当前目标：第 4 轮已完成；先阅读和理解 Schema 契约，再按触发规则进入第 5 轮 Repository

## 已完成内容

### 2026-07-22

- 完成第 1 轮代码生成：项目脚手架与运行入口
- 建立 FastAPI 应用入口、配置、日志、异常、枚举、身份透传上下文与健康检查路由
- 代码注释和文档字符串统一改为中文，并同步写入 `Agents.md`

### 2026-07-24

- 补充沉浸式调试协作规则
- 明确每轮代码生成都要附带至少 2 个有代表性的错误案例
- 要求错误案例必须包含日志定位、模块排查、原因分析、修复验证和正确输出样例

### 2026-07-29

- 完成第 2 轮代码生成：数据库基础与 ORM 骨架
- 建立 `app/db/base.py`、`app/db/session.py`、`app/models/base.py` 和模型聚合入口
- 建立统一的 SQLAlchemy Base、Session 工厂与通用 ORM 基类
- 使用 uv 建立项目专用 Python 3.12 环境，并生成 `pyproject.toml` 与 `uv.lock`

- 完成第 3 轮代码生成：核心数据模型
- 建立 `Document`、`DocumentChunk`、`ProcessingTask`、`DocumentEvent` 四个核心 ORM 模型
- 落实状态、版本、幂等、心跳、重试、事件流水等关键字段和约束
- 已创建 `沉浸式错误案例分析-第3轮.md`

### 2026-07-31

- 补充项目根目录 `README.md`
- README 作为后续持续迭代的统一入口说明文件

### 2026-08-13

- 深入阅读第 3 轮模型与错误案例，已理解索引、条件唯一索引、外键删除策略、双向 ORM 关系和带时区时间的职责边界
- 理解 SQLAlchemy Session 的“脏状态”：对象字段变更被 ORM 检测到后，才会在 `commit()` 时生成 `UPDATE`
- 理解 JSON 列表的原地修改需要 `MutableList` 跟踪；否则内存对象看似改变，Session 却不会标记对象为脏，造成静默未持久化
- README 已覆盖项目定位、当前进度、技术栈、目录结构、启动方式、开发约定和后续补充方向

- 完成第 4 轮代码生成：Pydantic Schema 与统一响应结构
- 建立 `app/schemas/common.py`：严格基础模型、UTC 时间、泛型分页、通用成功和错误响应
- 建立 `app/schemas/document.py`：上传元数据、列表过滤、文档列表/详情、chunk 查询与响应
- 建立 `app/schemas/task.py`：任务详情、状态快照、事件时间线和 SSE 数据载体
- 建立 `app/schemas/__init__.py`：提供稳定的 Schema 聚合导入入口
- `chunk_metadata` / `event_metadata` 已通过别名对外统一序列化为 `metadata`
- 已创建 `沉浸式错误案例分析-第4轮.md`

第 4 轮验证结果：

- `ruff check app/schemas` 输出 `All checks passed!`
- `ruff format --check app/schemas` 输出 `4 files already formatted`
- 模拟 ORM 对象可以转换为文档 chunk 和任务事件响应，输出中只有 `metadata`
- SSE 输入时间 `2026-08-13T10:00:00+08:00` 被规范为 `2026-08-13T02:00:00Z`
- 文档分页 `page_size=51`、重复标签、无时区时间和任务进度 `101` 均被 Pydantic 拒绝
- `ErrorResponse.details` 使用独立默认字典，不会在多个响应实例之间共享数据
- 上传响应只允许 `queued`，SSE 的 `success/failed` 事件会校验与任务状态的一致性
- 项目代码直接使用 Pydantic，已通过 `uv add pydantic` 将其明确记录为直接依赖并同步 `uv.lock`
- 全量 Ruff 检查发现第 3 轮学习注释被放入函数参数列表导致模型无法解析；已保留注释含义并移动到合法位置，四张 ORM 表回归导入通过

### 2026-08-15

- 理解 Pydantic 的 `Annotated` 类型：`UtcDateTime` 在 `datetime` 基础上附加 `AfterValidator`，将 Schema 时间字段的时区校验和 UTC 规范化复用到多个字段
- 进一步理解 `Annotated` 的职责：它只为基础类型附加元数据，具体校验行为由 Pydantic 读取其中的 `AfterValidator` 后执行
- 理解模块的 `__all__`：它声明模块希望公开的名称，主要控制通配符导入，并为后续聚合入口提供清晰的公共 API 边界
- 理解 ORM 模型与 Pydantic Schema 的分层：模型描述数据库存储、关系和约束；Schema 描述 HTTP 请求校验与响应输出，二者不能因字段相似而直接复用

### 2026-08-16

- 理解 Python 3.12 的泛型类语法：`class SuccessResponse[ResponseDataT]` 在类定义处声明类型参数，`ResponseDataT` 是 `data` 字段的类型占位符，不是需要单独定义的普通类
- 理解文档上传 Schema 的边界：`DocumentUploadRequest` 校验 multipart 表单中的业务元数据，FastAPI `UploadFile` 单独管理文件二进制流和资源生命周期

### 2026-08-17

- 理解 SSE 的项目用途：浏览器保持单向 HTTP 流连接接收文档异步解析的连接确认、进度、成功、失败和心跳事件，避免高频轮询任务详情接口
- 能区分任务持久化记录、文档事件流水和 SSE 推送消息：`TaskStatusResponse` 是任务状态快照，`TaskEventResponse` 是 `DocumentEvent` 的 API 输出，`TaskSseEvent` 是不必落库的实时传输载体
- 理解 `validate_event_status_consistency` 只校验单条 SSE 消息的 `event` 与 `data` 是否自洽，不会自动读取数据库或比对 `DocumentEvent`
- 理解事件时间线分页的用途：持久化 `DocumentEvent` 通过后续任务事件查询接口服务内部管理、审计、排障和 SSE 断线后的历史补齐；分页避免持续增长的流水被一次性查询和传输

### 2026-08-18

- 理解 Pydantic `AliasChoices("chunk_metadata", "metadata")`：输入校验时兼容 ORM 属性名和 API 字段名，`serialization_alias="metadata"` 再统一对外输出为 `metadata`

## 当前状态

- 第 1 轮、第 2 轮、第 3 轮和第 4 轮已完成
- 项目已经具备应用基础设施、数据库 Session、统一 Base、四个核心 ORM 模型，以及与 ORM 隔离的 API 请求响应契约
- 当前协作方式已经升级为“正确实现 + 独立错误案例 Markdown + 每一步输出演示 + 排障流程 + 修复验证 + 正确输出样例”的沉浸式学习模式
- 当前项目根目录已经补充 `README.md`，后续会随着代码生成持续迭代

## 下一步

- 进入第 5 轮：Repository 层骨架

重点文件：

- `app/repositories/document_repository.py`
- `app/repositories/chunk_repository.py`
- `app/repositories/task_repository.py`
- `app/repositories/event_repository.py`
- `app/repositories/__init__.py`

学习重点：

- Repository 如何隔离 SQLAlchemy 查询细节
- Repository、Service 和 Session 事务边界如何分工
- 如何实现文档分页过滤、版本查询和 chunk 批量写入
- 如何为后续任务原子抢占、心跳更新和事件时间线预留可靠接口
